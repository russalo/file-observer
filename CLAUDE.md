# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

File Observer — observation layer only. Recursively discovers files, extracts metadata and signals, emits a deterministic JSON manifest. Not responsible for ingestion, OCR, embeddings, or classification.

## Spec

- **NET CURRENT: SCANNER 1.40.0, LOGIC 1.21.0, SCHEMA 1.22**, chatlog mv 11, provenance mv 2, ai_session mv 2, lexicon mv 1, fact_block mv 1. Live on PyPI + GHCR. ~1404 passed (Linux); CI matrix green on ubuntu + macOS + Windows + a forced-no-libmagic job. (Run `pytest` for the exact count.) **v1.31.0 (2026-07-03, LIVE) = promotion pass: capture-metadata (the v1.16 image-namespace EXIF fields + the ENTIRE v1.17–1.20 `video` namespace) provisional→stable — the second promotion pass (after v1.23.0) and the “next pass, weeks out” v1.23.0 §6 deferred the capture fields to. Designation-only: manifest byte-identical (stability lives only in `--schema`), LOGIC unchanged 1.15.3, SCHEMA 1.16→1.17 (promotion = contract change). Ripe by the settled-logic+evidence criterion: parsers unchanged since ship, exiftool-oracle-validated, corpus-proven (the deliberately-built `wikimedia-exif` 70/70 make/model + `synth-video-capture` oracle-gated corpora), red-teamed (`test_capture_metadata_hardening`, 0 violations). Both `image` and `video` namespaces now fully stable (dims already stable since 0.5). HELD: `presentation`/`audio` (v1.24/1.25 — too young, season next pass), chatlog family (alpha-locked), `format_signatures`/`is_polyglot` (held-by-design). `test_v1_31.py` falsify-first; RFC-approved.** **v1.32.0 (2026-07-04, MERGED) = generic kv-fact-block specialist (FR #114): a new content-detected `fact_block` namespace (`pair_count`/`pairs`[{key,value}]/`duplicate_keys`) — when a text file's BODY (frontmatter stripped) is a `key: value` block, emit the pairs VERBATIM + GENERIC (never a per-consumer schema; recall's bright line — no validate/normalize). The FALLBACK observer for a text body no dedicated specialist owns (gated on `not requires_specialist_tool` → .eml/.pdf stay with their specialist). MEASURE-FIRST resolved FP-hygiene: a kv-line collides with dialogue → a sentence-value VETO (function-word+sentence-shape ≠ fact) is load-bearing (gate ≥3 pairs/ratio≥0.6/veto fires 497/497 Blueprint fact-blocks, 0/397 prose, 0/60 dialogue; without veto dialogue=100% FP). New `is_fact_block` flag; gate/veto→rules_hash, thresholds→static_tuning_hash. LOGIC 1.15.3→1.16.0 (new content-detection routing, additive False→True); SCHEMA 1.17→1.18 (new namespace, provisional). `test_v1_32.py` falsify-first; RFC-approved.** **v1.33.0 (2026-07-05) = AI-session observation increment 1 (FIRST of the AI-session arc, motivated by Russell wanting token usage + a linguistic/provenance signal): a new provisional `ai_session` namespace on is_chatlog-detected AI session logs (Claude Code / OpenAI / Gemini) carrying (a) token-usage SUMS (`usage`: turns_with_usage + canonical input/output/cache_read/cache_creation/reasoning/total, null-per-absent, vendor `raw_keys` verbatim, `truncated` flag) + (b) a producer-schema fingerprint (`vendor`/`surface`/`models`/`id_prefix`/`object_types`/`schema_mismatch`) anchored on id-prefix+object-type (NOT usage-key vocab — the MEASURE-FIRST real-log finding: OpenAI Responses/`/v1/responses` (codex) collides with Anthropic on `input_tokens`; 3 vendors/4 surfaces real-verified via self-gathered live logs). OBSERVE-ONLY: sums NEVER priced (the bright line; the whole federation stack is fo observes → trellis indexes → recall scopes+cites → interpreter judges). Full-file BOUNDED-DEVIATION read (64 MiB cap, `ai_session_full_file` trigger, OLE2/PDF precedent) — session logs far exceed the 64 KB window (real 1.6 MB session = 268 usage turns, ~7 in 64 KB → a windowed sum was silently ~2.6%). Fire gate = a RECOGNIZED token-usage block (rejects a dict merely named `usage` like {"cpu":5}). LOGIC 1.16.0→1.17.0 (values move manifest_checksum on AI-session corpora, v1.29 values-move precedent; NO routing flag flips), SCHEMA 1.18→1.19 (new namespace, provisional), new `ai_session` vector. FULL 4-leg (apparatus-asymmetry): leg-1 in-house swarm 12 (the FP-hygiene cluster on generic tree-wide anchors + 2 bounded/crash + the TRUNCATION headline) + leg-2 OpenAI gpt-5.3-codex (model-dedup + content-tiebreak; corpus-sum clamp DECLINED — clamping a true aggregate misreports it) + leg-3 sweep (workers byte-identical, NO-DRIFT, FP-hygiene 0/516) + leg-4 PR bots. `test_v1_33.py` falsify-first; RFC `docs/v1.33.0_RFC_Specification.md`.** **v1.34.0 (2026-07-07) = chatlog session axes (recall#62): the chatlog specialist emits THREE new provisional FLAT top-level scalars — `first_timestamp`/`last_timestamp` (min/max turn timestamp over the recognized-key set {timestamp,created_date,create_time,created_at}, parsed ISO-string-OR-epoch → CANONICAL ISO-8601 UTC `…Z`/ms; null-when-untimestamped) + `cwd` (first-seen top-level cwd, verbatim/bounded, null-when-absent). Deterministic pure function of the file (OBSERVE, don't derive; recall owns cwd→project + the bucketing). Unlocks BOTH #62 axes on the sourceless session node (time-bucket $gte/$lt + project-attr $prefix), ZERO trellis change (its flat pass surfaces them). MEASURE-FIRST grounded on real corpora: Claude Code `timestamp` Z/ms + top-level cwd (single-valued/session); oasst `created_date` +00:00/µs NESTED; ChatGPT `create_time` epoch-float; the format variance (Z vs +00:00, ms vs µs, epoch) makes normalization LOAD-BEARING (raw lexical min/max is wrong — the exact landmine, confirmed on 2 populated corpora). FULL-FILE bounded-deviation read (64 MiB, reuses the v1.33 ai_session read — my own real-data verify caught the 64 KB-window TRUNCATION: a 4.3 MB/2-day session reported a 5-min span until the temporal moved to full-file; existing chatlog signals stay windowed/byte-identical). 3-STAKEHOLDER CLEARED pre-build: recall (requester, flat-scalar spec), trellis (zero-change consume, verifies at ship), Sentinel (grep-verified concurs — reads no chatlog fields, golden test asserts only the speaker regex; widened-scope FYI for cwd). LOGIC 1.17.0→1.18.0 (values move on timestamped/cwd chatlog corpora, v1.29/v1.33 precedent; no routing flip, is_chatlog unchanged), SCHEMA 1.19→1.20 (3 new provisional fields), chatlog mv 10→11. `test_v1_34.py` falsify-first; RFC `docs/v1.34.0_RFC_Specification.md`; measure-first `scratch/measure_chatlog_timestamps_2026-07-07.md`.** **v1.35.0 (2026-07-08, LIVE) = AI-session per-model usage attribution (increment 3): new provisional `ai_session.usage_by_model` — a deterministic list (real models sorted, null bucket last) of per-model token-usage SUMS, keyed on the model CO-LOCATED with each usage dict (Claude message.model / OpenAI response.model / Gemini modelVersion, all 100% co-located). INVARIANT: session `usage` == elementwise sum of usage_by_model (the tested v1.33 session path untouched → byte-identical). Model verbatim incl markers; model-less usage-turns + hostile-cardinality overflow → the null bucket (sums stay complete). NEVER priced. LOGIC 1.18.0→1.19.0 (values move on ai_session corpora), SCHEMA 1.20→1.21 (new field, provisional), ai_session mv 1→2. `test_v1_35.py`.** **v1.36.0 (2026-07-09, LIVE) = defusedxml→purexml XML-dependency changeover (the dep-elimination lab's FIRST adoption): fo's XML hardening (OOXML/ODF specialists + structural XML-keys tier) moves to purexml (pure-stdlib, zero-dep, oracle-gated-to-defusedxml, MIT; 2695/0 on fo's corpus). fo OPTS INTO purexml's RECOMMENDED_LIMITS (max_depth=1000/max_attributes=256/max_bytes=100 MiB) → REJECTS pathologically-deep/oversized XML defusedxml parsed (LimitExceeded⊂ValueError → field degrades to null/error, never crashes). Parse byte-identical on real files; output changes ONLY on pathological XML → LOGIC 1.19.0→1.20.0 (v1.30.2 precedent). ScanContext records purexml+limits → manifest_checksum moves on EVERY manifest (dep record in every context, Pillar 1). SCHEMA frozen 1.21. `test_v1_36.py`.** **v1.37.0 (2026-07-10, LIVE) = MCP server (agent-native front-door, the adoption-track capstone): `file-observer-mcp` stdio entry + `[mcp]` extra exposing the EXISTING manifest/summary/schema via 4 read-only tools with progressive disclosure (`scan_summary` ~300 tokens = START HERE / `scan_file` / `scan_directory` guarded-by-file-count / `describe_surface`). A NEW SURFACE, checksum-identical to `scan()` → LOGIC/SCHEMA UNCHANGED (v1.26/v1.28 front-door precedent). stdio-local first (HTTP DEFERRED); optional `--root` allowlist. Prototype-first (measured context cost = the load-bearing check); route.sh review caught a DoS (guard-before-scan), scan_file-scans-parent (leg-1/leg-2 converged → temp-copy isolation), a read-only hardlink→copy P1 (Codex — hardlink bumps target ctime/link-count). `test_v1_37.py`.** **v1.38.0 (2026-07-10, LIVE) = bring-your-own-lexicon term observer (Russell-greenlit "bring your own it is"): fo counts a CONSUMER-supplied category-tagged lexicon's terms per text file → per-category counts+density (OBSERVE, never verdict) + a `lexicon_match` safety_flag on any hit. Know a file is a guardrail-trip risk BEFORE it reaches an AI (fo isn't an AI → safe to read what an LLM couldn't). VALUES-NEUTRAL engine (benign placeholders ONLY in code/tests — I must NEVER generate/curate sensitive terms; I'd trip my own guardrails); the real terms are runtime config, never committed, never echoed into the manifest OR error logs (only counts + category names + a content-hash `dictionary_id` = sha256 of canonical-JSON, moves on any term change → catches silent drift; the reserved-since-v0.9 dictionary_id finally realized — first concrete customer-defined-vector). MEASURE-FIRST: word-boundary token(-sequence) match NOT substring (substring over-counts 9.4× — scunthorpe); full-file bounded read (64 MiB — a risk term can sit anywhere in a long log); streaming finditer + length-keyed bounded window (safe on untrusted docs — no O(n) token list, per-token work independent of lexicon size); literal terms only → ReDoS-safe; fail-loud bounded parse. DORMANT when no lexicon → existing manifests byte-identical (checksum moves only for lexicon-supplied scans, v1.30 gated precedent). New provisional `lexicon_match` namespace (lexicon_id/categories/total_hits/total_tokens) + `lexicon` vector (mv 1) + `lexicon_match` safety_flag + `lexicon_full_file` trigger. LOGIC 1.20.0→1.21.0, SCHEMA 1.21→1.22. FULL 4-leg caught 10 real issues (textbook apparatus-asymmetry): leg-1 (specialist_metadata clobber on .eml+lexicon), leg-3 NO-DRIFT, leg-4 PR bots carried the SERIOUS ones (a dictionary_id HASH COLLISION defeating drift-detection → canonical JSON; an unbounded-memory matcher → streaming; a TERM-LEAK in error logs → category+reason only; the schema-vector-table drift — fact_block/ai_session vectors were ALSO never in `--schema`, a stale `test_all_six_vectors_listed` enshrined it). leg-2 gem-pro config BROKEN. `test_v1_38.py`; RFC `docs/v1.38.0_RFC_Specification.md`.** **v1.38.1 (2026-07-11, patch, HISTORY-only) = `--version`/`-V` CLI flag (#126): prints `file-observer X.Y.Z`, exit 0 — un-breaks downstream version probing (recall's doctor read the usage banner → false "below floor"; recall#71 = the recall-side half). Runtime-only output, manifest UNCHANGED (--workers/--watch/--stdout precedent) → LOGIC/SCHEMA frozen.** **v1.39.0 (2026-07-11, LIVE) = MCP front-door gains lexicon + delta (the v1.37 MCP server was one release behind the scanner): now threads the v1.38 lexicon via a SERVER-STARTUP `--lexicon <path>` flag (mirrors --root) + delta via a `previous_manifest_path` tool param (honors --root). THE PRIVACY LANDMINE (the load-bearing design call): a tool ARG is constructed by the calling LLM, so a terms-inline param would put the private terms INTO the agent's context — backwards; the startup flag keeps terms in a config file the agent never sees, only term-free counts cross the wire. scan_summary's `notable` gains per-category lexicon hits + delta counts. A front-door: manifest byte-identical to a same-settings CLI scan → LOGIC 1.21.0 / SCHEMA 1.22 FROZEN (v1.26/v1.28/v1.37 precedent). Reviewed with review-kit's NEW `mcp-audit.sh` MCP-SAST leg (0 real findings — its rules assume a NETWORK server vs fo's stdio-local read-only model; "noisy, not gospel") + route.sh (leg-1/CodeRabbit CONVERGED on --root-restricting the delta path — fixed). `test_v1_39.py`; RFC `docs/v1.39.0_RFC_Specification.md`.** **v1.40.0 (2026-07-12) = field trust classification + `--trusted-only` safe mode + `--schema` trust annotation (#136; Phase 1 of the r/mcp consumption-safety plan, Phase 0 = the "consume fo safely" docs #137): the manifest is a report ABOUT untrusted input — it echoes attacker-controllable strings verbatim (path/content_preview/extracted metadata), and a consumer (esp. an LLM agent) had no machine-readable way to tell fo signal from quoted attacker bytes nor to ask fo for ONLY the safe signal. A first-class `FIELD_TRUST` registry tags every field fo_derived (trusted — numbers/booleans/hashes/enums/flags, can't carry a text payload even when file-sourced like page_count/width) vs file_derived (untrusted — verbatim free-text bytes: path/filename/content_preview/tags/frontmatter/metadata strings); FAIL-SAFE (unsure ⇒ file_derived; a completeness guard asserts every field is classified). `--trusted-only` (CLI / `ScannerConfig(trusted_only=True)` / MCP tool param + server flag) emits a PROJECTION nulling the file_derived fields + a top-level `trusted_only: true` marker + a fo-derived `path_id = sha256(rel-posix path)` correlation handle. **THE LEG-1 SELF-REVIEW CATCH (the load-bearing fix):** the first cut projected `files[]` ONLY — file paths / author names / the summary prose still leaked through the manifest-LEVEL blocks (confirmed empirically: a real path via `quality.duplicate_clusters` on the examples/ tree). Fixed by projecting the WHOLE manifest — also scrubbing `meta.source_dir`→null, top-level `summary`→null, `delta.*`/`duplicate_clusters[].paths`→[], `per_directory_summary[].directory`→null, `vectors_collected[].summary`→{} (D4 fail-safe: drop the free-text-laced corpus rollup, keep the fo-derived vector envelope); MCP `scan_summary` (a compact dict outside the manifest projection) also nulls its path/summary. `--schema` gains a per-field `trust` annotation. A PROJECTION — no new observation, NO sanitization → DEFAULT manifest byte-identical, LOGIC 1.21.0 / SCHEMA 1.22 FROZEN (the v1.26/v1.28/v1.37 front-door precedent); `schema_doc_version` bumps for the annotation; SCANNER 1.39.0→1.40.0. Falsify-first `test_v1_40.py` (per-file + manifest-level canary batteries, red-verified; 1421 passed); RFC `docs/v1.40.0_RFC_Specification.md`.** **Capture-metadata arc (v1.16 image EXIF → v1.17 video container → v1.18 video device+GPS) complete; v1.19 = human-readable refresh, v1.20 = `video.creation_date_qt`, v1.21.0 = content-aware text recognition.** **Since v1.21 (2026-06-19): v1.22.0 = content-aware recognition extended to BINARY (`unsupported_extension` now means "couldn't identify it" for ANY file type, not "extension unlisted" — Candidate A, recognition-only, LOGIC 1.11.0→1.12.0); v1.22.1 = `.eml` MIME-guard relaxation (libmagic-text-typed mail was wrongly skipped → email corpus 13→21/21, LOGIC →1.12.1); v1.23.0 = promotion pass, `preservation` field + vector provisional→stable (designation-only, manifest byte-identical, SCHEMA 1.13→1.14); v1.23.1 = PDF-header FP fix (find-anywhere `%PDF-` bounded to a 256-byte header window in the MIME sniff via the C1/C2 split — surfaced by puresniff's clean-room sweep, LOGIC 1.12.1→1.12.2); v1.23.2 = corroborated PDF-header sniff (window widened 256→1024 + structural-token corroboration, generalizing the FP fix to match the scanner's own 1024-byte tolerance; LOGIC 1.12.2→1.12.3); v1.23.3 = bzip2 dual-magic + `_OneOf` byte-alternation matcher (recognizes empty/data-less bzip2 via the end-of-stream magic at offset 4; rejects prose + invalid level; reconciled 0/0 with the puresniff clean-room replica; LOGIC 1.12.3→1.12.4). **v1.24.0 (2026-06-25, LIVE on PyPI) = office & media EXTRACTION (Candidate B phase 1): new specialists for `.pptx`/`.odp` (new `presentation` namespace: slide_count/title/author/application) / `.odt` (document) / `.ods` (spreadsheet) / `.jp2` (dims) / `.tiff`/`.tif` (dims+EXIF), reusing the docx-xlsx ZIP+XML + v1.16 image-EXIF + v1.17 ISOBMFF + TIFF-IFD machinery (stdlib, NO new dep; never-crash/bounded); `requires_specialist_tool` flips for the 7 new types → LOGIC 1.12.4→1.13.0, SCHEMA 1.14→1.15. Full 4-leg review (leg-2 ran via the NEW `gem-pro.sh`/Antigravity pro path after the consumer Gemini OAuth sunset).** **v1.25.0 (2026-06-26, LIVE on PyPI) = audio & legacy presentation EXTRACTION (Candidate B phase 2): new `audio` namespace (`format`/`bitrate`/`duration_s`/`title`/`artist`/`album`/`year`) for `.mp3` — ID3v2.2/2.3/2.4 tags + a bounded MPEG frame-header parse (Xing VBR or CBR estimate); stdlib, NO new dep. THE lone net-new untrusted-binary parser in Candidate B — carried at the v1.8.1 never-crash/bounded bar from commit one (capped reads/sync-search/frame loops, hostile-size rejection; `test_capture_metadata_hardening.py` extended). Legacy `.ppt` reuses the existing `presentation` fields via OLE2 `SummaryInformation`+`DocumentSummaryInformation` (the `.doc`/`.xls` olefile path — no new field). Audio MIME guard TIGHT (`audio/mpeg` only). `requires_specialist_tool` flips for `.mp3`/`.ppt` → LOGIC 1.13.0→1.14.0, SCHEMA 1.15→1.16. Completes Candidate B (graduated out of the candidate tier; CAD stays active). 4-leg: leg-2 Gemini-inline UNAVAILABLE (tier-exhausted) but leg-4 `gemini-code-assist` bot covered the Gemini family (clean); leg-4 Codex 2×P2 — ID3 extended-header FIXED, `.ppt` bounded_sample-provenance fixed in the v1.25.1 follow-up.** **v1.25.1 (2026-06-26, LIVE on PyPI) = OLE2 full-file deviation provenance (patch, HISTORY-only): the OLE2 path-reading specialists (`.doc`/`.xls`/`.msg`/`.ppt`) read property streams from anywhere in the compound file via `olefile`, NOT the 8 KB sample, but reported `trigger="bounded_sample"`+`sample_size` (false) — now declare `trigger="bounded_deviation"`+`reason="ole2_full_file_required"`, budget null. Provenance-accuracy ONLY (extracted values byte-identical, asserted per-format) but `signal_provenance` feeds `manifest_checksum` → it moves for OLE2-bearing manifests. Fixed the whole OLE2 family at once (pre-existing, family-wide). LOGIC 1.14.0→1.14.1 (manifest-surface change, the v1.8.2/v1.9.1 precedent: a non-routing output-value change that moves `manifest_checksum` bumps LOGIC); SCHEMA unchanged 1.16. New `generated.msg` fixture (MAPI substg streams) added for the `email`-namespace coverage (leg-4/gemini test-completeness). 1164 tests.** **Post-release doc audit (2026-06-26): the unguarded PROSE format lists/claims had drifted (README "Supported specialist formats" frozen ~v1.0; a stale "OLE2 .doc/.xls carry no application" claim, false since v1.10; CLAUDE.md per-RFC `current` marker). Fixed + added a THIRD drift-guard.** THREE committed-doc drift-guards fail CI if docs lag the code: `test_readme_version_references_current` (README versions) + `test_contract_docs_version_references_current` (PUBLIC_CONTRACT §3 + CONVENTIONS §1.1) + **`test_readme_lists_every_specialist_format`** (every `SPECIALIST_TOOLS` extension must appear in the README "Supported specialist formats" list — closes the unguarded-prose-list gap the audit found). **CLAUDE.md + LIMITATIONS prose are still NOT guarded — refresh by hand each release (CLAUDE.md had drifted to 1.21.2; refreshed to 1.24.0 on 2026-06-25; to 1.25.1 / 1.26.0 / 1.27.0 on 2026-06-26; to 1.28.0 / 1.28.1 on 2026-06-27; to 1.29.0 on 2026-06-28; to 1.30.0 on 2026-06-30; to 1.30.1 / 1.30.2 on 2026-07-01).** ~1238 tests. **v1.26.0 (2026-06-26, LIVE on PyPI) = one-call public API (FIRST aim of the adoption-by-lowering-barriers track): `from file_observer import scan; m = scan("./folder")` + `scan_to_json(path)→str`, thin wrappers over `Scanner(Path(p), ScannerConfig(enable_specialists=…, workers=…, **kwargs)).scan()`. `specialists`/`workers` friendly aliases; canonical `enable_specialists=` accepted too (pops to avoid alias collision — leg-4/gemini). The import/Python API is NOT under the manifest contract (v1.0.1 precedent) → purely additive ergonomics, `scan(p)` byte-identical to the explicit path (falsify-first `test_v1_26.py`, 9). README quickstart leads with `scan()`; legacy `scanner` shim re-exports via `import *`. LOGIC unchanged 1.14.1; SCHEMA unchanged 1.16 (no manifest change — the v1.9 `--workers`/v1.11 `--watch` precedent); SCANNER 1.25.1→1.26.0. ALSO fixed the PyPI project-page broken images: README `<img>` were repo-relative (`docs/assets/*.png`) which GitHub serves but PyPI can't resolve → switched to absolute `raw.githubusercontent.com/.../main` URLs (+ absolute LICENSE-badge link); doc-nav links stay relative (branch-aware on GitHub). leg-1 swarm clean; leg-4 gemini caught the alias collision (fixed); no corpus sweep/red-team (manifest unchanged, no new parser).** **v1.27.0 (2026-06-26, LIVE on PyPI) = JSON Schema artifact (SECOND adoption-by-lowering-barriers aim, the most FO-shaped): a committed, GENERATED `docs/manifest.schema.json` (JSON Schema draft 2020-12) describing the manifest, for any-language validation/codegen — emitted by `--schema --schema-format json-schema`. Generated from the manifest dataclasses (single source of truth, can't drift); stable core typed strictly (`additionalProperties:false`), namespace-keyed/provisional dicts open objects, `vectors_collected` items typed as the VectorRecord envelope (summary open). `$id` carries schema_version (build-time interface; pin the MAJOR). Decision C (generate + validation guard): `test_v1_27.py` asserts committed==regenerated AND a real manifest validates (specialists on/off, empty, chatlog, delta, signed); `jsonschema` is a DEV-ONLY dep (runtime stays zero-dep). Describes the manifest → changes nothing emitted. LOGIC/SCHEMA unchanged (1.14.1/1.16); SCANNER 1.26.0→1.27.0. leg-1 swarm clean; leg-4 Codex (vectors_collected envelope typing) + gemini (README dup) both adopted.** **v1.28.0 (2026-06-27, LIVE on PyPI) = `--stdout` (manifest to stdout, no file/report) — pipe-friendly for Docker/pipelines (`fo . --stdout | jq`); the prerequisite for the Docker-image step but a pipeline win on its own; aligns FO's one-shot output with --watch/--schema. Output routing ONLY (manifest byte-identical, written as UTF-8 BYTES to stdout.buffer — leg-4/gemini cross-platform/non-ASCII safety; byte-identical to the encoding=utf-8 file); mutually exclusive with --output/--watch/--schema (exit 2; --schema parity = leg-4/Codex). LOGIC/SCHEMA unchanged (1.14.1/1.16); SCANNER 1.27.0→1.28.0. leg-1 clean.** **Docker image + uvx/pipx docs SHIPPED (PR #105, 2026-06-27, docs+infra, NO version bump): `Dockerfile` (python:3.12-slim + libmagic1 + all specialist extras, builds FROM SOURCE — reproducible/no-PyPI-race, non-root, `CMD --stdout .`) + `.github/workflows/docker-publish.yml` (GHCR on release + workflow_dispatch, built-in GITHUB_TOKEN, no external secret) + README uvx/pipx/docker install docs. Built+smoke-tested locally; leg-4 (Codex PyPI-race→build-from-source, gemini WORKDIR-before-USER, Codex output-self-inclusion) all adopted. ✅ DONE (2026-06-27): the first image was built+pushed (workflow_dispatch v1.28.0 → `:1.28.0`+`:latest`) and the GHCR package made PUBLIC + repo-connected. **Verified live: `docker pull ghcr.io/russalo/file-observer:1.28.0` (no auth) + `docker run -v …:/data:ro` returns a clean manifest.** `docker pull ghcr.io/russalo/file-observer` now works for anyone, zero Python. Registry = GHCR (Docker Hub deferred = a 2nd workflow target + DOCKERHUB_TOKEN if ever wanted). Future releases auto-build+tag the image (release run derives the version from the tag).** **v1.28.1 (2026-06-27, LIVE on PyPI) = `[all]` install extra (patch, packaging-only, HISTORY-only no-RFC): `pip install "file-observer[all]"` = one line for every optional specialist (union of [yaml,msg,security,pdf,watch]; self-referencing so it can't drift; [dev] now references [all]; drift-guard `test_all_extra_covers_every_runtime_extra`). No scanner/manifest/API change (v1.0.1/v1.0.2 precedent); needs a release because an extra only reaches PyPI when a version carrying it is published. LOGIC/SCHEMA frozen (1.14.1/1.16); SCANNER 1.28.0→1.28.1. leg-4 gemini's exact-parse drift-guard suggestion adopted. NOTE: the docker-publish workflow AUTO-FIRED on this release (build+push `:1.28.1`, 38s) — confirms the on:release auto-image design works.** **GitHub Action SHIPPED (PR #107, 2026-06-27, infra+docs, NO version bump): `action.yml` (composite — installs `file-observer[all]` into an isolated venv, runs `<path> <args> --stdout`, outputs `manifest-path`; inputs via env to avoid injection; cross-platform python/Scripts-vs-python3/bin) for `uses: russalo/file-observer@<tag>` in CI + a matrix self-test (`uses: ./` on ubuntu+windows). The windows self-test EARNED ITS KEEP (v1.15 OS-matrix lesson): caught a real Windows action bug (`pip.exe` can't self-upgrade → use `python -m pip`) + confirmed the manifest is UTF-8 end-to-end. leg-4 gemini-HIGH (Windows venv layout) + Codex-P2 (write-outside-scan-tree to avoid self-inclusion) both adopted. README has the Action usage block.** **🎉 THE ADOPTION-BY-LOWERING-BARRIERS TRACK IS COMPLETE: produce (v1.26 `scan()`), pipe (v1.28 `--stdout`), consume (v1.27 JSON Schema), run-anywhere (Docker GHCR + uvx/pipx), one-line-install (v1.28.1 `[all]`), CI-native (GitHub Action). Six releases v1.26→v1.28.1, all additive, contract frozen (SCHEMA 1.16 / LOGIC 1.14.1 unchanged since the audio arc). No NEXT adoption item committed — Russell steers what's next.** **v1.29.0 (2026-06-28, LIVE on PyPI + GHCR) = chatlog detection recognizes AGENTIC (tool-turn) sessions — the FIRST cut of the parked chatlog work, motivated by `recall` (the new `chatlogs/recall/` chatlog-search node spun up this session — a 2nd consumer of the chatlog domain beside Sentinel). A turn counts when it has a conversational role + a text-bearing block OR a distinctive agentic block (`thinking`/`tool_use`/`tool_result` = new `CHATLOG_AGENTIC_BLOCK_TYPES`); detection requires ≥1 PROSE turn so pure tool-execution/RPA logs don't FP; generic `image`/`document` are NOT triggers (incl. their captions). **Measure-first on recall's real federation corpus:** 3/28 large session logs FN'd (incl. a 139MB log), 0 doc FP; root cause = text-centric counting (NOT the read window — 2 false leads ruled out by grounding first). Both detection AND turn-counting signals adopt it (prose signals stay text+thinking; the §4 split); the block sets feed the chatlog `rules_hash`. **`is_chatlog` STRICTLY ADDITIVE (False→True, never True→False — superset gate); chatlog signal VALUES (turn_count/speaker counts) + identity_digest MOVE for agentic logs → `manifest_checksum` moves on agentic corpora.** LOGIC 1.14.1→1.15.0, chatlog mv 9→10, SCHEMA unchanged 1.16, SCANNER 1.28.1→1.29.0. **FULL 4-leg, each leg catching a DIFFERENT bug class (the apparatus-asymmetry pattern):** leg-1 in-house swarm (2 HIGH — untyped-text True→False regression + image/document FP → set narrowed to distinctive-only, reversing the §8.1 "include document" call on FP evidence); leg-2 Gemini 3.1 PRO via `gem-pro.sh`/agy (3 fixed — pure-tool-log FP → the ≥1-prose rule, sibling-`message`-prose loss, image-caption FP; 3 REFUTED by repro — claimed crash/nested-miss/frozenset-nondeterminism; flash timed out, PRO covered); leg-3 corpus sweep (26/28, 0 regress, workers byte-identical); leg-4 bots (gemini-HIGH + Codex-P2 CONVERGED on an unhashable-`type` `TypeError` on malformed JSON → never-crash coerce-guard). PR #108; 1215 tests; CI matrix green. **This is item #1(A) of the chatlog backlog — the un-gated tool-turn FN fix surfaced BY recall as a 2nd consumer; (B) the prose/markdown non-count redesign stays RPG-corpus-gated.** **v1.30.0 (2026-06-30, LIVE) = CLI robustness — the FIRST two issues through the new GitHub-issues consumer-feedback model (bestiary's operational shake-out, #109/#110). #109: fail-loud (`rc=2`) on a nonexistent/non-dir source + `--workers<1` + `--preview-max<0` (missing `--previous-manifest` WARNS) instead of a silent `rc=0` empty manifest — the silent-zero cascade that empties trellis's index + recall's search at the ROOT of the fo→trellis→recall stack (bestiary's day-tally named "silent-wrong on invalid-but-plausible input" as the federation's dominant finding-class; gazetteer/keyward resist by failing loud+field-named — fo now joins them); empty-REAL-dir preserved (0 files is a valid observation). #110: default output moves OUT of the installed package dir (`<site-packages>/file_observer/manifests/` — breaks read-only/container installs, pollutes a pip-managed dir) to `./file-observer-manifests/` in the cwd; `-o`/`--stdout` unchanged → trellis/recall (pass `-o`) UNAFFECTED. **leg-1 review caught a self-inclusion regression** (a bare re-scan of the cwd would observe its own output) → discovery now SKIPS a `file-observer-manifests/` dir (the tool's reserved output name; the only LOGIC-bearing part). Valid scans byte-identical EXCEPT a tree containing that dir, now skipped (`test_v1_30.py`: CLI-`-o` checksum == `scan()` + re-scan-of-cwd excludes prior output). **LOGIC 1.15.0→1.15.1** (the discovery skip — `manifest_checksum` moves only for a tree with that dir; the v1.8.1 symlink-skip precedent), SCHEMA frozen 1.16, SCANNER 1.29.0→1.30.0. leg-3 sweep NO-DRIFT (corpora lack the dir). ~1226 tests.** **v1.30.1 (2026-07-01, LIVE) = self-inclusion skip ANCHORED to the actual output dir — a RED-TEAM HARDENING patch, the FIRST from the new OpenAI review leg (`gpt.sh`, review-kit — a 3rd cognition-decorrelated family beside the Claude in-house swarm + Gemini; it found what leg-1 under-weighted — leg-1 caught the self-inclusion CLASS but my v1.30.0 fix over-corrected, and leg-1 rated the mkdir LOW). Two real defects in v1.30.0's OWN new code: ① the skip matched a BARE dir name at ANY depth → an UNRELATED user dir named `file-observer-manifests` was SILENTLY DROPPED (data loss; also missed on case-insensitive fs); ② the now-cwd output write wasn't wrapped → a non-writable output path TRACEBACKED. Fix: anchor the skip to fo's actual RESOLVED output dir (normcase prefix-match on its rel path, via a runtime `skip_output_dir` excluded from meta.config) — ONLY fo's own output skipped, API never skips (writes nothing), case-insensitive miss fixed by the resolved anchor; + wrap the output write → clean `rc=1`, not a traceback. `manifest_checksum` moves ONLY for a tree with an unrelated such dir (now included); sweep NO-DRIFT. LOGIC 1.15.1→1.15.2, SCHEMA frozen 1.16, SCANNER 1.30.0→1.30.1; the v1.8.1 red-team-hardening precedent. review-kit issue #11 filed (gpt.sh deeper models need v1/responses). ~1233 tests.** **v1.30.2 (2026-07-01, LIVE) = ReDoS / bounded-TIME hardening — the *bounded-size-but-unbounded-time* member of the bounded-observation class the v1.8.1 (size/crash/escape) + capture-metadata passes never covered. fo caps input SIZE (`baseline_max_bytes`, 64 KB default / 1 MB deep) but 3 content regexes backtracked SUPER-LINEARLY on bounded-but-pathological input, so a single crafted `.md`/`.txt` could hang a per-file scan: `CHATLOG_WIKI_LINK_RE` (`\[\[.+?\]\]` → ~13 s on 64 KB of `[[`), `ASSET_RE` (~1.7 s on 64 KB of `[`), `PROVENANCE_VERSION_SUFFIX_RE` (`\s+`/`\s*` overlap → hung on a 64 KB-whitespace crafted PDF `/Producer`; found by a falsify-first ALL-regex battery, NOT manual read). Bounded/anchored → linear (≤53 ms); new standing guard `test_regex_redos_hardening.py` battery-tests EVERY compiled regex (generic walk = 77, not the 37 globals) under a hard per-regex timeout in a child process (batched fast-path, per-regex naming on failure) so the class can't regress/reappear. Behavior byte-identical on real content (parity: wiki links, markdown assets, real producer strings); observed VALUES change only on bracket-bearing / over-long (label>1024 / target>4096=PATH_MAX — no valid on-disk path excluded) / >32-whitespace pathological inputs; **reference_tokens mv 2→3** (its rules-definition string carried the actual wiki pattern → updated + mv bumped, so `rules_hash`/mv move in vectors_collected → `manifest_checksum` moves for EVERY reference_tokens-bearing manifest — the fingerprint contract; the provenance fingerprint is live-derived and moved automatically); sweep NO-DRIFT + workers byte-identical. LOGIC 1.15.2→1.15.3, SCHEMA frozen 1.16, SCANNER 1.30.1→1.30.2; the v1.8.1 red-team-hardening precedent. **Surfaced by the fo↔bruni seam audit** (the same O(n²)-regex class Bruni had — carried home to fo's own extractors, which had a WORSE instance). leg-2 OpenAI `gpt.sh` caught 3 real improvements (ASSET `\n`-exclusion regressed multiline links → bounds-only; guard's hardcoded table names didn't exist → generic walk covers all 77; child exit-code check); leg-4 Codex 2×P2 (the stale reference_tokens rules-fingerprint → mv 2→3; the 400-char target cap → PATH_MAX) — ALL adopted + re-validated. leg-3 sweep NO-DRIFT. ~1238 tests.** Russell steers next.**
- `docs/v1.40.0_RFC_Specification.md` — **current release spec (2026-07-12). Field trust classification + `--trusted-only` safe mode + `--schema` trust annotation (#136; Phase 1 of the r/mcp consumption-safety plan). A first-class `FIELD_TRUST` registry tags every manifest field fo_derived (trusted — counts/booleans/hashes/enums/flags) vs file_derived (untrusted — verbatim attacker-controllable free-text: path/filename/content_preview/tags/frontmatter/metadata strings); `--trusted-only` emits a projection nulling the file-derived fields across the WHOLE manifest (the leg-1 self-review caught the first cut leaking through the manifest-LEVEL blocks — duplicate_clusters paths, per_directory dirs, source_dir, the summary prose, vector summaries) + a fo-derived `path_id` handle; `--schema` annotates each field's trust. A PROJECTION, no new observation, no sanitization → DEFAULT manifest byte-identical, LOGIC 1.21.0 / SCHEMA 1.22 FROZEN (v1.26/v1.28/v1.37 front-door precedent); `schema_doc_version` bumps. Falsify-first + red-verified. (Full detail in the NET CURRENT line above.)**
- `docs/v1.39.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-11). MCP front-door gains lexicon + delta: the v1.37 MCP server now threads the v1.38 lexicon (server-startup `--lexicon` flag — the private terms never cross the MCP wire, only term-free counts; a tool ARG is LLM-constructed so a terms-inline param would leak them into the agent's context) + delta (`previous_manifest_path` tool param, honors --root). A front-door: manifest byte-identical to a same-settings CLI scan → LOGIC 1.21.0 / SCHEMA 1.22 FROZEN. Reviewed with review-kit's new `mcp-audit.sh` MCP-SAST leg. (Full detail in the NET CURRENT line above.)**
- `docs/v1.38.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-10). Bring-your-own-lexicon term observer: fo counts a CONSUMER-supplied category-tagged lexicon's terms per text file → per-category counts+density (observe, never verdict) + a `lexicon_match` safety_flag. Values-neutral engine (benign placeholders only); terms are runtime config, never in the manifest/logs (only counts + a content-hash dictionary_id → catches silent drift). Word-boundary not substring (9.4× FP); full-file bounded read; streaming length-keyed matcher (safe on untrusted docs). New provisional `lexicon_match` namespace + `lexicon` vector. LOGIC 1.20.0→1.21.0, SCHEMA 1.21→1.22. First concrete customer-defined-vector. (Full detail in the NET CURRENT line above.)**
- `docs/v1.37.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-10). MCP server (agent-native front-door): `file-observer-mcp` stdio + `[mcp]` extra exposing the existing manifest/summary/schema via 4 read-only tools (progressive disclosure). A new SURFACE, checksum-identical to `scan()` → LOGIC/SCHEMA UNCHANGED. (Full detail in the NET CURRENT line above.)**
- `docs/v1.34.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-07). Chatlog session axes (recall#62): three new provisional flat chatlog scalars — `first_timestamp`/`last_timestamp` (min/max turn timestamp → canonical ISO-8601 UTC) + `cwd` (first-seen). Full-file bounded-deviation read; unlocks recall's time-bucket + project-attr axes on the sourceless session node, zero trellis change. LOGIC 1.17.0→1.18.0, SCHEMA 1.19→1.20, chatlog mv 10→11. (Full detail in the NET CURRENT line above.)** *(v1.35/1.36/1.38.0 RFCs exist too; v1.36 = purexml changeover, v1.35 = ai_session per-model usage.)*
- `docs/v1.33.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-05). AI-session observation increment 1: new provisional `ai_session` namespace on is_chatlog-detected AI session logs (Claude/OpenAI/Gemini) — token-usage SUMS (full-file bounded-deviation read; NEVER priced) + a producer-schema fingerprint anchored on id-prefix+object-type. LOGIC 1.16.0→1.17.0, SCHEMA 1.18→1.19. Four-leg review complete. (Full detail in the NET CURRENT line above.)**
- `docs/v1.32.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-04). Generic kv-fact-block specialist (FR #114): new provisional `fact_block` namespace emitting a text body's `key: value` pairs verbatim+generic (content-detected fallback; sentence-value veto keeps it off dialogue). LOGIC 1.15.3→1.16.0, SCHEMA 1.17→1.18. (Full detail in the NET CURRENT line above.)**
- `docs/v1.31.0_RFC_Specification.md` — **prior (SHIPPED 2026-07-03). Promotion pass — capture-metadata (image EXIF + the entire `video` namespace) provisional→stable.** Designation-only: manifest byte-identical (stability lives only in `--schema`). The second promotion pass (after v1.23.0) and the “next pass, weeks out” v1.23.0 §6 deferred the capture fields to; ripe by the settled-logic+evidence criterion (parsers unchanged since ship, exiftool-oracle-validated, corpus-proven, red-teamed). Both `image` and `video` namespaces now fully stable. Held: presentation/audio (too young), chatlog (alpha-locked), format_signatures/is_polyglot (by design). LOGIC unchanged 1.15.3; SCHEMA 1.16→1.17 (promotion = contract change). (Full detail in the NET CURRENT line above.)
- `docs/v1.30.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-30). CLI robustness — fail-loud on bad input (#109) + sane default output dir (#110).** fo errors (`rc=2`) on a nonexistent/non-dir source + `--workers<1` + `--preview-max<0` (missing `--previous-manifest` warns) instead of a silent `rc=0` empty manifest; default output moves to `./file-observer-manifests/` (cwd) out of the installed package dir, AND discovery skips that dir so a re-scan doesn't self-include (the leg-1 catch). LOGIC 1.15.0→1.15.1 (the discovery skip only), SCHEMA frozen 1.16, SCANNER 1.29.0→1.30.0. Surfaced by bestiary via the GitHub-issues feedback model. (Full detail in the NET CURRENT line above.)
- `docs/v1.29.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-28). Chatlog detection recognizes AGENTIC (tool-turn) sessions.** A turn counts on a text-bearing OR distinctive-agentic block (`thinking`/`tool_use`/`tool_result`); detection requires ≥1 prose turn (pure tool logs don't FP); generic `image`/`document` excluded. Recovers tool-heavy Claude Code logs the text-centric gate FN'd (surfaced by `recall`, a 2nd chatlog-domain consumer). `is_chatlog` additive; chatlog signal VALUES + identity_digest move for agentic logs. LOGIC 1.14.1→1.15.0; chatlog mv 9→10; SCHEMA unchanged 1.16; SCANNER 1.28.1→1.29.0. (Full detail in the NET CURRENT line above.)
- `docs/v1.28.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-27). `--stdout` (manifest to stdout).** Pipe-friendly one-shot output (no file/report), the Docker-image enabler; output routing only, manifest byte-identical (LOGIC/SCHEMA unchanged 1.14.1/1.16; SCANNER 1.27.0→1.28.0). (Full detail in the NET CURRENT line above.)
- `docs/v1.27.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-26). JSON Schema artifact for the manifest.** A committed, generated `docs/manifest.schema.json` (draft 2020-12) for any-language validation/codegen, via `--schema --schema-format json-schema`. Generated from the dataclasses; describes the manifest (LOGIC/SCHEMA unchanged 1.14.1/1.16; SCANNER 1.26.0→1.27.0). (Full detail in the NET CURRENT line above.)
- `docs/v1.26.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-26). One-call public API (`scan`/`scan_to_json`).** First aim of the adoption-by-lowering-barriers track; Python-surface ergonomics only, manifest byte-identical. LOGIC/SCHEMA unchanged (1.14.1/1.16); SCANNER 1.25.1→1.26.0. (Full detail in the NET CURRENT line above.)
- `docs/v1.25.0_RFC_Specification.md` — **prior (SHIPPED 2026-06-26, + the v1.25.1 provenance patch). Audio & legacy presentation extraction (Candidate B phase 2).** New `audio` namespace (`format`/`bitrate`/`duration_s`/`title`/`artist`/`album`/`year`) for `.mp3` (ID3 + bounded MPEG frame parse) + legacy `.ppt` reusing the `presentation` fields via OLE2. LOGIC 1.13.0→1.14.0; SCHEMA 1.15→1.16. (Full detail in the NET CURRENT line above.)
- `docs/v1.24.0_RFC_Specification.md` — **prior. Office & media extraction (Candidate B phase 1).** New `presentation` namespace + extraction for `.pptx`/`.odp`/`.odt`/`.ods`/`.jp2`/`.tiff`/`.tif`. LOGIC 1.12.4→1.13.0; SCHEMA 1.14→1.15.
- `docs/v1.23.0_RFC_Specification.md` — **prior. Promotion pass — `preservation` → stable** (designation-only). The per-`FileRecord` `preservation` field + the `preservation` vector graduate provisional→stable; stability lives only in `--schema`, so the manifest is byte-identical. Also reclassified `format_signatures`/`is_polyglot` as **held-by-design** (the signature vocabulary intentionally grows on every MIME change — never a promotion candidate). LOGIC unchanged (1.12.1); SCANNER 1.22.1→1.23.0; **SCHEMA 1.13→1.14**. **Patch on top (HISTORY-only, no RFC): v1.23.1 PDF-header FP fix — find-anywhere `%PDF-` bounded to a 256-byte header window for the MIME judgment (C1, `_sniff_mime`) while `scan_signatures` keeps find-anywhere so embedded-PDF polyglots stay honest (C2, `is_polyglot`); surfaced by puresniff's clean-room sweep, loose since v1.3; LOGIC 1.12.1→1.12.2.**
- `docs/v1.22.0_RFC_Specification.md` — **prior. Content-aware recognition extended to BINARY (Candidate A).** `unsupported_extension` now fires ONLY when content genuinely couldn't be identified (octet-stream / extension-fallback / unreadable), for ANY file type — NOT when identified-but-no-specialist. Recognition-only, byte-identical extraction; the honest completion of the v1.21 text arc. LOGIC 1.11.0→1.12.0; SCANNER 1.21.x→1.22.0; SCHEMA unchanged 1.13. **Patch: v1.22.1 `.eml` MIME-guard relaxation — accept text/plain & text/html for `.eml` (libmagic types body-dominated mail as text, not message/rfc822); extension-gated so a lying text `.msg` stays distrusted; email corpus 13→21/21; LOGIC →1.12.1.**
- `docs/v1.21.0_RFC_Specification.md` — **prior.** **Content-aware text recognition** (no new extraction — recognition only, the v1.15.1 pattern). `unsupported_extension` no longer fires on recognized text — the diagnostic now means **"couldn't identify it,"** not "extension not in our list." From the v1.20 candidate scan: ~9k corpus files (`.py`/`.ini`/`.svg`/source/config/docs) were flagged + counted *degraded* though the full text baseline already ran on them — they just weren't *listed* in `SUPPORTED_EXTENSIONS`. **Decision (RFC §6): Option B (content-aware)** over A (a curated list). Gate = `_is_recognized_text(mime) AND mime_prov.trigger != "extension_fallback" AND not read_failed AND (unicode-BOM OR looks_like_text(sample))` — recognition rests on **observed content** (`text/*` or `RECOGNIZED_TEXT_APP_MIMES` = json/js/ecmascript/xml/yaml/svg+xml/`inode/x-empty`), NOT the extension-fallback MIME (would tie the flag to the platform `/etc/mime.types`), NOT read-failed, BOM-rescues UTF-16, printable-ratio vetoes a lying `text/plain` over NULs. Empty files recognized (`inode/x-empty` = positively-identified-empty). **No-libmagic path stays flagged** (accepted §6.2a gap, enforced by the trigger arm). fastapi residual 186→4 (all genuine git binaries, 0 text misses). **LOGIC 1.10.0→1.11.0** (routing + supported/degraded counters); SCANNER 1.20.0→1.21.0; **SCHEMA unchanged 1.13**. 4-leg, each leg hitting the SAME gate from a different angle: leg-1 supported-count stat-failure-early-return + missing `requires_libmagic` markers; leg-2 Gemini read-failure "recognized" hole + UTF-16-BOM rejection; leg-3 sweep clean (workers byte-identical, zero drift); leg-4 Codex caught a doc-vs-code mismatch (RFC §6.2a said fallback stays flagged but the first cut recognized `.py` via `mimetypes`) → the content-derived-MIME gate. **Patches on top (HISTORY-only, no RFC per [[feedback_rfcs_for_minors_only]]): v1.21.1 provisional-designation correction; v1.21.2 `--schema --format json` envelope-shape commitment (PUBLIC_CONTRACT §1.12, versioned by `schema_doc_version`).**
- `docs/v1.20.0_RFC_Specification.md` — **prior. Video QuickTime creation date — `video.creation_date_qt`.** A NEW `video` field surfacing the Apple **`com.apple.quicktime.creationdate`** key (`moov`→`meta`→`keys`/`ilst`): the capture moment **with timezone** (e.g. `2026-06-16T05:25:55-0700`), raw/as-is. **Kept SEPARATE from v1.17's `creation_date`** (mvhd, UTC, file-finalization) — never reconciled (observe-don't-reconcile; both surfaced). Motivated by a real AirDrop finding: `mvhd` is *file-write* (perturbed by save/transfer) while the key holds the *shutter moment* with a zone `mvhd` lacks. Extends v1.18's `_qt_keys` walk — one key lookup, no new I/O/parse/dependency. Oracle-validated `exiftool`-exact on the real iPhone 11 + 16 Pro Max corpus. **LOGIC 1.9.0→1.10.0** (new extracted value); SCANNER 1.19.0→1.20.0; **SCHEMA 1.12→1.13** (a new field — a contract-shape change, unlike a new *value* of an existing field).
- `docs/v1.19.0_RFC_Specification.md` — **prior.** **Human-readable surfaces refresh** (no change to what is observed). **(A)** the per-scan `summary` (`_build_summary`, was frozen ~v1.10) now names the `provenance` vector, adds a **Capture** line (geotagged count + capture devices from image/video make+model), breaks the safety-flag count into **named flags**, surfaces `preservation`, and **comments on the ambiguous** (content-vs-extension MIME mismatches + polyglots — observe-don't-interpret). **(B)** new **`--schema --format summary`** — a human-readable PROSE rendering of the build's complete observable surface (the readable counterpart to `--schema --format json|md`), walking the same v1.13 introspection registries (single source of truth → can't drift; completeness-guarded). **(A) changes the `summary` string → feeds `manifest_checksum` → LOGIC 1.8.0→1.9.0** (v1.8.2-class determinism surface); SCANNER 1.18.0→1.19.0; **SCHEMA unchanged 1.12** (no new field; B is a separate surface). leg-2 Gemini caught `schema_to_summary` dropping `specialists.tools` (fixed); leg-4 Codex caught a provenance undercount (`len(toolchains)`→`distinct_toolchains`, fixed). Validated on a **19,508-file full-battery shakedown** (workers byte-identical, 0 fatal).
- `docs/v1.18.0_RFC_Specification.md` — **prior. Video capture device + GPS-presence (closes the capture arc).** The `video_structure` specialist gains the Apple QuickTime keys: `make`/`model` (`moov`→`meta`→`keys`/`ilst`) + `gps_present`/`gps_source` (`location.ISO6709` — presence + mechanism, NOT coordinates) → `geotagged` fires for video. Handles BOTH `meta` forms (QuickTime `.mov` +8 AND ISO `.mp4` +12 — the leg-2/Gemini catch). Validated `exiftool`-exact on 2 real iPhone 16 Pro Max clips (gitignored). Apple-first (Android `udta`/`©xyz` deferred — needs a sample; QuickTime `creationdate` key = revisit candidate). LOGIC 1.7.0→1.8.0; SCANNER 1.17.0→1.18.0; SCHEMA 1.11→1.12.
- `docs/v1.17.0_RFC_Specification.md` — **prior. Video capture-metadata — container/track half (MP4/MOV/M4V).** A new `video_structure` specialist reads ISOBMFF container metadata (`moov`→`mvhd`/`trak`→`tkhd`/`mdia`→`hdlr`/`stsd`), stdlib + `struct`, no library: `codec` / `duration_s` (float = duration/timescale) / `width`/`height` (the `vide`-track `tkhd`, 16.16 fixed) / `creation_date` (`mvhd` 1904-epoch → ISO 8601 UTC via timedelta). `.mp4/.mov/.m4v` → it (new `video` namespace; `requires_specialist_tool` True). **`moov`-at-TAIL handled** (measured 61/62 real `.mov`): `_read_moov` scans top-level boxes + **seeks past `mdat`**, reading only `moov` (bounded by `MOOV_MAX_BYTES`=16 MiB; declared `bounded_deviation`/`moov_box_beyond_sample`). **Measure-first + oracle: matched `exiftool` EXACTLY on 61/61 real `.mov`** (exiftool = standalone dev-oracle at `scratch/tools/exiftool`, never shipped). **Split from the v1.16 RFC** — a new namespace/specialist/SCHEMA surface is a minor, not a 1.16.x patch. **iPhone-specific make/model (Apple QuickTime keys) + GPS-presence GATED** on a real iPhone-`.mov` corpus (0/62 conformance coverage) → **v1.18** (drafted `docs/v1.18.0_RFC_Specification.md`, gated). LOGIC 1.6.0→1.7.0; SCANNER 1.16.0→1.17.0; SCHEMA 1.10→1.11. leg-1 caught a v1-mvhd 64-bit duration-sentinel wrong-value + a pre-1970 date Linux/Windows divergence (fixed); leg-2 Gemini clean; leg-4 bot bounds-guards adopted.
- `docs/v1.16.0_RFC_Specification.md` — **prior. Image capture-metadata — EXIF for JPEG & HEIC.** Image specialist (was dimensions-only) reads EXIF: `make`/`model`/`orientation`/`datetime_original` (CIPA DC-008 TIFF/IFD0 + Exif sub-IFD via 0x8769), **GPS-PRESENCE** (the 0x8825 GPS-IFD ptr — *presence NOT coordinates*) → `image.gps_present` + new **`geotagged` safety_flag**, and `xmp_present`. EXIF via JPEG **APP1** / HEIC **ISO 14496-12 `meta`→`iinf`/`iloc`→`Exif`-item** (version-aware `iloc`). **`.heic/.heif/.avif` route to the image specialist** (`requires_specialist_tool` True — completing v1.15.1 recognition with extraction). **HEIC dims from EXIF `PixelXDimension`/`PixelYDimension`** — authoritative; the `ispe` box on tiled iPhone HEICs is a 512px tile, deliberately NOT used (honest-null beats a wrong tile size). Stdlib + `struct`, no Pillow, no new dep. EXIF reads a **bounded 1 MiB head** (declared deviation). The `_safety_extras` merge (v1.12, PDF-only) **generalized to all specialists** so `geotagged` reaches `safety_flags`. LOGIC 1.5.2→1.6.0; SCANNER 1.15.2→1.16.0; SCHEMA 1.9→1.10. leg-1 caught a HIGH iloc-count DoS (the v1.8.1 bounded-observation lesson); leg-2 Gemini caught a gap in that fix (zero-size extent spin) + a `_jpeg_dimensions` past-SOS wrong-value; both fixed.
- `docs/v1.15.0_RFC_Specification.md` — **prior.** **Cross-platform hardening.** Added a CI OS matrix (ubuntu + macOS + Windows) + a deterministic forced-no-libmagic job — the matrix was the highest-yield reviewer, finding bugs no other leg could. **HEIC/HEIF/AVIF detection** (iPhone photos no longer mislabeled `video/mp4` on the no-libmagic path; brand@offset-8 signatures before the generic ftyp rule; ftyp-superseding block to avoid false image+video polyglots). **Root-cause Windows fix: `import magic` (python-magic) HANGS on a Windows box with no libmagic DLL** (import-time library search blocks, not raises) → `import file_observer.scanner` itself hung → **python-magic now excluded on Windows via a `platform_system` marker** (the v1.3 pure-Python fallback engages; `python-magic-bin` for libmagic-grade MIME). **`iter_files` now sorts by `p.as_posix()`** (WindowsPath sorts case-insensitively → file-order determinism break — Pillar 1). Deterministic `extension_mime`: registered `.toml`→text/plain, `.yaml`/`.yml`→application/yaml, Office types, fills macOS/Windows nulls. `pytest-timeout` + job `timeout-minutes` as permanent hang nets. **LOGIC 1.4.3→1.5.0** (MIME tier); SCANNER 1.14.0→1.15.0; SCHEMA unchanged 1.9. **NOT byte-identical across OS** (platform is a ScanContext field; contract is identical bytes + identical ScanContext → identical records). **v1.15.1 (patch, HISTORY-only)** — HEIC/HEIF/AVIF *recognition*: `.heic/.heif/.avif` added to `SUPPORTED_EXTENSIONS` (no more `unsupported_extension`/degraded) + registered extension MIMEs + per-brand precision (generic `heif/mif1/msf1`→`image/heif`, not `image/heic`); **no specialist → `requires_specialist_tool` stays False** (recognition ≠ extraction — extraction is v1.16). LOGIC 1.5.0→1.5.1; SCHEMA 1.9. Closes the v1.15.0 leg-4 codex P2 items (#2/#3); codex's compatible-brands suggestion declined → v1.16. **DISCIPLINE: adding any surface element MUST add it to the relevant registry (`--schema` completeness tests) AND regenerate `docs/SCHEMA.md`.** **A MIME-detection change is LOGIC (v1.3 precedent); registering an extension MIME is no-op on platforms that already know it. python-magic is now Windows-conditional — never re-add it as an unconditional dependency.**
- `docs/v1.14.0_RFC_Specification.md` — **prior.** Promotion pass (2nd, after v1.10) + `--schema` stability annotation. Promoted provisional→stable: `pdf.parser` (on the data — the v1.10 hold-reason was a misattribution; `parser` records *which tier ran*, not extracted values), `{document,spreadsheet}.application`, the `provenance` vector. Held: chatlog family (alpha-locked) + `preservation`. `--schema` now annotates `stability` on every field/vector. Designation-only — manifest byte-identical; LOGIC frozen 1.4.3; SCHEMA 1.8→1.9.
- `docs/v1.13.0_RFC_Specification.md` — **prior.** **`--schema` self-description capability.** `file-observer --schema [--schema-format json|md]` introspects the installed build and emits its COMPLETE output surface (manifest fields via dataclass introspection, specialists + their metadata fields, vectors, safety_flags, error codes, signal_provenance triggers, format signatures, preservation tiers, subcategories, MIME tiers) and exits without scanning — a SEPARATE surface, manifest byte-identical to v1.12.x. Closes the **self-evidencing-but-not-self-describing** gap (a consumer could read a manifest but not know what the build *could* emit without reading source). **Framing:** the introspective refactor IS the minor — making the surface enumerable from one place; `--schema` is the thin read on top. New registries: `PROVENANCE_TRIGGERS` (39), `SAFETY_FLAGS` (5), `ERROR_CODES` (10), `SPECIALIST_FIELDS` (6 ns). Committed `docs/SCHEMA.md` = generated `--schema --format md` (drift-guarded; **regenerate on any surface change** — see CONVENTIONS release checklist). Falsify-first paid out: the completeness cross-check caught 3 triggers missing from the registry (`match`/`registry_none`/`content_pattern_none` — `else`-arms of conditional expressions a literal-grep + first-AST-guard both missed → guard strengthened to walk `IfExp`). The v1.12.2 patch (error-code centralization) was the de-risk step. **LOGIC unchanged 1.4.3; SCHEMA unchanged 1.8** (manifest contract frozen; `--schema` is a new surface). SCANNER 1.12.2→1.13.0. 849 passed, 1 skipped. Net current: **SCANNER 1.13.0, LOGIC 1.4.3, SCHEMA 1.8**, chatlog mv 9, provenance mv 2. **DISCIPLINE: adding any surface element** (new field/vector/specialist/safety_flag/error code/provenance trigger) MUST add it to the relevant registry (the completeness tests in `test_v1_13.py` enforce that `--schema` stays complete against a real scan) AND regenerate `docs/SCHEMA.md`.
- **v1.12.2 (patch, HISTORY.md only).** Error-code centralization: `xml_parse_failed` / `toml_parse_failed` moved from inline literals to `ERR_*` constants. Byte-identical output. The de-risk step ahead of v1.13 `--schema`. AST guard `test_no_inline_error_code_literals` asserts no inline error-code literal remains (the v1.13 trigger guard mirrors it). SCANNER 1.12.1→1.12.2.
- **v1.12.1 (patch, HISTORY.md only — patches don't get RFCs per [[feedback_rfcs_for_minors_only]]).** **Red-team hardening patch**, same-week post v1.12.0; mirrors the v1.8 → v1.8.1 precedent. 19 attack vectors across 5 surfaces (encryption decode, uncompressed xref-stream, cascade merge, transient marker plumbing, cryptography fallback) + cross-cut determinism + v1.8.1-guard re-verification. **One real Pillar-1 break, caught in two stages:** my red-team round caught the *crash* class — `ScanContext.dependencies[*].version` stored `__version__` raw → non-string `__version__` crashes `json.dumps`; my fix coerced via `str()` but PR-bot Codex (P2) caught that bare `object()` `str()` returns `"<object at 0x...>"` embedding a memory address → still non-deterministic across processes. Final fix: `_dep_version_str()` helper coerces via `str()` AND rejects angle-bracket-bounded results (the universal shape of default object reprs); applied to all 7 dependency entries (magic, chardet, yaml, olefile, defusedxml, pypdf, cryptography). Real version strings pass through unchanged; hostile shapes normalize to `"unknown"`. **18 other vectors confirmed robust** — the two v1.12.0 leg-1 review rounds closed most of what red-team would otherwise have exposed (narrower yield than v1.8.1's 6 findings). Pure hardening — **LOGIC unchanged 1.4.3; SCHEMA unchanged 1.8**. SCANNER 1.12.0→1.12.1. 825 tests (was 806; +19 new in `test_v1_12_1.py`). **Sweep baseline re-anchored to v1.12.1** (workers=1-vs-4 byte-identical on every corpus). Net current: **SCANNER 1.12.1, LOGIC 1.4.3, SCHEMA 1.8**, chatlog mv 9, provenance mv 2. **Apparatus track record extended:** leg-1 review catches bugs in the patch's logic (5 + 12 on v1.12.0); red-team catches bugs the patch never had logic for (1 on v1.12.1); leg-4 PR-bots catch incomplete fixes (Codex P2 on the v1.12.1 fix). Each leg cognition-decorrelated.
- `docs/v1.12.0_RFC_Specification.md` — **prior release spec**. **Post-v1.5 PDF arc residual closure** — owner-permission-locked encrypted PDFs + uncompressed xref-stream support. **Falsify-first vindicated TWICE:** (i) the RFC originally framed §3.3 as PNG predictor extension (avg/paeth/TIFF); the §6.1 measurement returned **0 predictor-gap PDFs on corpora_infra** — pivoted to uncompressed-xref support before any code was written. (ii) the leg-1 in-house swarm review caught **5 critical bugs** the other 3 legs (gemini, corpus sweep, PR bots) missed — including a `page_count` overwrite bug breaking the byte-identical contract and a **wrong permission bit** check; patched pre-PR. See RFC §12. **(A) Owner-locked decode:** v1.11 dispatch had a `not meta["encrypted"]` gate at scanner.py:3082 that skipped the cascade; pypdf already handles empty-password decrypt. v1.12 relaxes the gate and surfaces **`extraction_permission_bypassed`** as a new well-known value on **`FileRecord.safety_flags`** (stable cross-format list[str] since v0.7) — observe-with-disclosure for PDFs where the primary EXTRACT bit (ISO 32000 Table 22 bit 5, `UserAccessPermissions.EXTRACT` value 16) is NOT in `user_access_permissions` but metadata is extracted anyway. The original v1.12 code checked `EXTRACT_TEXT_AND_GRAPHICS` (bit 10, the deprecated accessibility override) — leg-1 #15 caught it. **No SCHEMA bump** — disclosure lives on `safety_flags`, not a new field (leg-1 #12 — right altitude). Caltrans 2025 PDFs have bit 5 CLEARED → bypass disclosure correctly fires on them (2 files in corpora_infra). **(B) Uncompressed xref streams:** `_pdf_xref_stream_map` previously assumed Flate; no-filter case (42 PDFs on corpora_infra; e.g. WSDOT specs) handled via raw-body branch + aggregate `total_raw >= PDF_INFLATE_CAP` cap. `/Filter` detection is regex key-match (`re.search(rb"/Filter\s*[/\[]", d)`), not substring (leg-1 #14). **`cryptography` registered in `ScanContext.dependencies`** parallel to pypdf (Pillar-1 — leg-1 #3/#8); new `ERR_PDF_ENCRYPTION_UNSUPPORTED` emitted when crypto absent on AES-encrypted PDF. **Corpus delta on corpora_infra:** page_count null 2→0; producer null (pypdf path) 3→1; `extraction_permission_bypassed` in safety_flags on 2 files (Caltrans pair). 653 non-residual PDFs byte-identical to v1.11. workers=1-vs-4 byte-identical (v1.9 Layer-A gate). **Pure specialist extraction → LOGIC unchanged 1.4.3.** SCANNER 1.11.0→1.12.0; **SCHEMA unchanged 1.8**. 804 tests. **Two rounds of leg-1 review:** round-1 caught 5 critical bugs (page_count overwrite, wrong permission bit, cryptography missing from ScanContext, no-filter oracle gate, strict-vs-equal cap); round-2 on the patched code caught 12 new bugs the round-1 patch itself introduced (parser mis-attribution on signal-only dicts, ERR_PDF_ENCRYPTION_UNSUPPORTED invisible to aggregate counters, bypass-only escape short-circuiting stdlib, doc drift). Both patches landed pre-PR; the patch needed its own patch — RFC §12 + §13. **Consumer caveat:** the `provenance` and `author_aggregate` vector `identity_digest` will move on corpora containing AES-encrypted PDFs (the cascade now fills producers/authors that feed those corpus-vectors). Net current: **SCANNER 1.12.0, LOGIC 1.4.3, SCHEMA 1.8**, chatlog mv 9, provenance mv 2.
- `docs/v1.11.0_RFC_Specification.md` — **prior**. **`--watch`** opt-in continuous trigger loop: rescan on FS events, emit each scan's delta as one JSONL line on stdout. **No change to what is observed** — each emitted scan is byte-identical to a one-shot `file-observer` invocation at the same FS state. `LOGIC` and `SCHEMA` stay frozen (same lesson as `--workers N` in v1.9). Use-case-anchored: Sentinel's NPC / item / lore watchers (and any equivalent downstream interpreter) read the deltastream and fan their schemas at the consumer layer; file-observer stays one-pass per event. **Measure-first** (RFC §6.1, `scratch/measure_watch.py`): watchfiles vs watchdog → **watchfiles wins** on the chatlogs corpus (1:1 event delivery vs watchdog's 6× amplification; 43ms per-event latency, 53ms cold-start). End-to-end on `chatlogs/` (~200MB, 19 files): ~50ms debounce + ~2.1s rescan ≈ **2.2s near-realtime** per chat input. Decisions locked: §3.1 debounced batch; §3.2 watchfiles (new `file-observer[watch]` extra); §3.3 stdout JSONL; §4.6 files[] excluded by default with `--watch-include-files` opt-in. Kept-warm process pool reused across rescans (`_scan_paths_parallel` gains an `external_pool=` hook); SIGTERM/SIGINT graceful shutdown; never-crash carried into the watch loop (per-rescan error → stderr log, loop continues). SCANNER 1.10.0→1.11.0; SCHEMA + LOGIC unchanged.
- `docs/v1.10.0_RFC_Specification.md` — **prior**. **Provisional-lifecycle** release: the first promotion pass since v0.11 + a liberal new-intake stance + a new **`candidate` tier** below provisional. **Promoted → stable:** `duplicate_clusters` family, `specialist_stats`, `errors[].detail`, `pdf.text_detected`, `pdf.xref_type` (criterion: *settled producing logic*, not age). **New provisional:** `preservation` (closed-table format-obsolescence, lookup-only), `application` extended to OLE2 `.doc`/`.xls` (feeds `provenance`, mv 1→2; real-data: 5 corpus `.doc` → "Microsoft Word"), summary top-authors. **`candidate` tier:** held observations (CAD, EXIF, word-twisting) tracked + sweep-instrumented, **never in the manifest** — ladder `candidate → provisional → stable`. **Held from promotion:** chatlog family, `pdf.parser`, `provenance` (extended this release). SCANNER 1.9.1→1.10.0; SCHEMA 1.7→1.8 (promotions = contract change, v0.11 precedent); **LOGIC unchanged 1.4.3**.
- `docs/v1.9.0_RFC_Specification.md` — **prior**. Opt-in **parallel scan (`--workers N`)** + a stderr progress indicator. A *performance* change with **byte-identical output regardless of worker count** — so **LOGIC + SCHEMA unchanged**; that unchanged LOGIC is the falsifiable contract (`tests/test_v1_9.py`). Concurrency model resolved by a **measure-first** prototype (RFC §6.1, `scratch/measure_workers.py`): a **process pool** (3.4–5.4× per-file pass; threads net-negative — GIL-bound + thread-unsafe libmagic cookie — declined; hybrid moot). The load-bearing change is making **`scan_file` pure** (no instance-state mutation); corpus counters (`filename_patterns`/`reference_tokens`) derived from the records afterward; `--workers`/`--progress` are runtime-only (excluded from `meta.config`). Validated: workers=1-vs-N **byte-identical on 964 real PDFs, 3.12× end-to-end**. SCANNER 1.8.2→1.9.0 (v1.9.1 patch = stat-failure path fix, LOGIC→1.4.3).
- `docs/v1.8.0_RFC_Specification.md` — **prior**. Per-format parser fork: decode object-stream PDFs (page tree compressed in an `/ObjStm`) that v1.7 left `page_count=null`. Tiered cascade: **`pypdf` (tier 1, optional `file-observer[pdf]`) → stdlib in-house decoder (tier 2: zlib + PNG predictor + `/W` xref + `/ObjStm`) → null** — recovery works WITH or WITHOUT the dep. Decision (RFC §4): **B with an A fallback**, measured-first. pypdf-as-oracle showed 300/300 recoverable + flawless oracle (282/282 v1.7 agreement); the stdlib decoder is gated by **exact agreement with pypdf** (0 disagreements on 371 corpus PDFs; recovers 328, nulls 43 scoped-out — never wrong). New provisional `pdf.parser` (pypdf/stdlib/none). **Pure specialist extraction — LOGIC unchanged (1.4.0)**; additive (null→value, 0 change to v1.7 values). SCANNER 1.7.0→1.8.0; SCHEMA 1.6→1.7. Coverage on `corpora_infra` (was 355): **653/655 with pypdf, 616/655 without**; producer 320→602. Closes the post-v1.5 three-minor arc (v1.8.1/1.8.2 patches followed — see roadmap).
- `docs/v1.7.0_RFC_Specification.md` — prior. Structural-anchor reader: follow the PDF's own index (`startxref` → latest trailer → root catalog → page tree), parsing the classic xref table for object offsets + following `/Prev` across incremental updates. New provisional `pdf.xref_type`; load-bearing `STRUCTURAL_ANCHORS` dispatcher. **LOGIC unchanged (1.4.0).** SCANNER 1.6.0→1.7.0; SCHEMA 1.5→1.6. `xref_type` revealed 57% object-stream (the v1.8 population).
- `docs/v1.6.0_RFC_Specification.md` — prior. Production-provenance dimension: a corpus-scoped `provenance` vector (normalized `toolchains` via a closed table, `production_years`, `digitization` born_digital/scanned/ocr_detected) modeled on `author_aggregate` — complements it (that's WHO authored; this is WHAT-TOOL / WHEN / digitization). Reuses v1.5 `requires_vision`/`text_detected` + OCR-producer fingerprints; cross-format via PDF producer + OOXML `app.xml`. **Pure aggregation — LOGIC unchanged (1.4.0).** SCANNER 1.5.0→1.6.0; SCHEMA 1.4→1.5 (additive vector); `provenance` method_version 1. Validated on `corpora_infra` (reproduced the Wayne-K facts; re-scan also fixed two PDF-string bugs — UTF-16BE literals, version-suffix strip).
- `docs/v1.5.0_RFC_Specification.md` — prior. PDF specialist head+tail read + `requires_vision` fix. The PDF specialist read only the 8 KB head, but a PDF's page tree (`/Count`), `/Info` dict (producer), and font markers live in the trailer at the file END, and born-digital content streams are compressed — so `page_count`/`producer` were null for ~all real PDFs and `requires_vision` mis-flagged born-digital PDFs as needing vision (same bug class as pre-v0.7.1 OLE2). Fix (stdlib, no new dep): markers (`text_detected`/`requires_vision`) over a fixed head+128 KB-tail window; `page_count`+`/Info` over the **whole file** (capped 64 MB). `page_count`=max `/Count` **anchored to `/Type /Pages`** (searched to `endobj`; un-anchored max over-counted `/Outlines` bookmarks); `/Info` depth-aware literal (balanced+escaped parens)+hex, gated on `/Encrypt`; new provisional `pdf.text_detected`; `requires_vision` only when no-text AND image markers. Validated on the 964-PDF infra corpus: **requires_vision 310→12, page_count 9→353, producer 9→320**. Object-stream PDFs (PDF 1.5+) return `page_count=None` (honest null, never wrong). SCANNER 1.4.0→1.5.0; LOGIC 1.3.0→1.4.0; SCHEMA 1.3→1.4 (additive `pdf.text_detected`).
- `docs/v1.4.0_RFC_Specification.md` — prior. Content-shape chatlog detection gate, ADDITIVE over the retained v1.2.4 machinery (the non-speaker stop-list is load-bearing — falsification proved the draft's "replace it" premise wrong). The prose rule gains `utterance_ratio≥0.6` — a turn is an *utterance* when it has a **function word** (the discriminator: natural turns have them, atomic data values don't), ends in sentence punctuation, is multi-word, or is long — rejecting cyclic data tables (`Item:`/`Price:`). Plus FP-lexicon dominance (`Added:` changelogs), version-tag structure vote-against (release notes; dated journals don't vote against), FAQ complete-set exclusion (`{question,answer,q,a,faq}`; `A:`/`B:` survives). A density floor was prototyped then dropped in review (FN'd multi-line dialogue) — `density` is surfaced, not gated. New provisional `content_shape` (`utterance_ratio`/`density`). On real data == v1.2.4 (0 diff/3,032 files); changes only adversarial inputs. Accepted FNs: ultra-terse contentless dialogue, all-distinct roll-call, `Q:`/`A:` interviews. SCANNER 1.3.0→1.4.0; LOGIC 1.2.0→1.3.0; SCHEMA 1.2→1.3 (additive `content_shape`); chatlog method_version 8→9.
- `docs/v1.3.0_RFC_Specification.md` — prior. Pure-Python content-based MIME fallback (no libmagic): `detect_mime` gains a magic-signature tier (`_sniff_mime`) between libmagic and the extension fallback; `MAGIC_SIGNATURES` generalized to a multi-constraint matcher + expanded to ~24 formats incl. RIFF→WebP/WAV/AVI disambiguation (generic `riff_container` retained for `format_signatures`, suppressed when a sub-type matches). New provenance trigger `magic_signature_fallback`. SCANNER 1.3.0; LOGIC 1.1.4→1.2.0; SCHEMA 1.2 (unchanged).
- `docs/v1.2.0_RFC_Specification.md` — prior (patches v1.2.1–v1.2.4 against it — HISTORY.md, no RFC). Chatlog generalized & hardened detection + per-speaker structure (provisional); patches tightened chatlog FP rules.
- `docs/v1.1.0_RFC_Specification.md` — prior. Corpus Intelligence: `quality.duplicate_clusters` + `quality.specialist_stats` (both provisional).
- `docs/v1.0.0_RFC_Specification.md` — prior. Schema freeze. Public contract binding. Backward compatibility policy.
- `docs/archive/0.x/v0.11.0_RFC_Specification.md` — prior release. Provisional → stable promotions, SECURITY.md.
- `docs/archive/0.x/v0.10.0_RFC_Specification.md` — prior release. Human-readable scan summary, author_aggregate corpus vector, filename_patterns vector.
- `docs/archive/0.x/v0.9.0_RFC_Specification.md` — prior release. Vector abstraction (identity digest, rules_hash, static_tuning_hash), `vectors_collected[]` manifest block, chatlog vector (refactored from v0.8), reference_tokens vector (7 subcategories), email body chatlog cross-cut, per-directory aggregation, Dublin Core adoption. All v0.9 additions provisional.
- `docs/archive/0.x/v0.8.0_RFC_Specification.md` — prior release. Chatlog specialist (first content-detected, not extension-based): is_chatlog flag, drift-visible signals (turn counts, speaker labels, section markers, reference tokens, top capitalized tokens, vocabulary estimate).
- `docs/archive/0.x/v0.7.0_RFC_Specification.md` — v0.7.x line. XLS specialist, spreadsheet `format` field, safety_flags, ScanQuality block. v0.7.1 and v0.7.2 are patch releases against this spec — see HISTORY.md.
- `docs/archive/0.x/v0.6.0_RFC_Specification.md` — configurable depth (specialist_budget, extension_overrides, profiles), structural signatures, polyglot detection, integrity envelope (HMAC manifest_signature).
- `docs/archive/0.x/v0.5.0_RFC_Specification.md` — schema reshape: namespaced specialist_metadata, schema_version field, baseline_max_bytes, cross-platform hardening.
- `docs/archive/0.x/v0.4.0_RFC_Specification.md` — semantic specialist tool naming, deviation policy, coverage expansion (JPEG, EML, XLSX, MSG enrichment).
- `docs/archive/0.x/v0.3.0 RFC_Specification.md` — base contract: capability-locked determinism, signal layering, provenance, bounded observation.
- `docs/HISTORY.md` — running index of all versions and patch releases. Start here when orienting.
- `docs/CONVENTIONS.md` — internal naming, version-bump rules, document promotion paths, tracking inventory of specialists / namespaces / signatures / safety flags / error codes.
- `docs/PUBLIC_CONTRACT.md` — consumer-facing stability commitments. **Binding as of v1.0.**

RFC normative language applies (MUST/SHOULD/MAY per BCP 14). Read the v1.0 RFC plus HISTORY.md before making changes.

## Package and distribution

| | |
|---|---|
| **Package name** | `file-observer` |
| **CLI commands** | `file-observer` or `fo` (shorthand) |
| **PyPI** | https://pypi.org/project/file-observer/ |
| **GitHub** | https://github.com/russalo/file-observer (public) |
| **License** | AGPL-3.0 + dual commercial (see LICENSE) |
| **Copyright** | Russell Pfister |
| **Publishing** | GitHub Release triggers `.github/workflows/publish.yml` → PyPI via trusted publisher |

## Workbench (shared working surface)

The project uses Tether for human-AI collaboration. The workbench serves editable JSON docs in a browser over tailnet.

- **Tether repo:** `/srv/projects/pkplab/tether/` (see BOOTSTRAP.md and WORKBENCH.md there)
- **Scanner workbench data:** `scratch/workbench/` (gitignored)
- **Hub port:** 8800 (manages all project instances on this node)
- **Scanner instance port:** 8801
- **Tailnet access:** `http://origin-core:8800` (hub) / `http://origin-core:8801` (scanner workbench)
  - Full DNS: `origin-core.taild63637.ts.net`
  - Short: `origin-core` (works inside tailnet via search-domain)
  - IPv4: `100.89.175.30`

**After a server reboot**, the workbench must be restarted:
```bash
cd /srv/projects/pkplab/tether
nohup python3 hub.py --port 8800 > /tmp/workbench-hub.log 2>&1 &
curl -s -X POST http://localhost:8800/api/start \
  -H 'Content-Type: application/json' \
  -d '{"project": "scanner", "data_dir": "/srv/projects/pkplab/scanner/scratch/workbench", "port": 8801}'
```

**Future:** systemd unit for auto-start on boot (pattern: `/etc/systemd/system/blog.service` on origin-core). Manual restart is acceptable while iterating.

## Chatlog home

This project's Claude Code session jsonl files live at **`/srv/projects/pkplab/chatlogs/scanner/`** (transitioned 2026-06-05). The default `~/.claude/projects/-srv-projects-pkplab-scanner/*.jsonl` paths are symlinks back into that shared dir — Claude Code is unaware of the move, the actual storage is unified. See `/srv/projects/pkplab/chatlogs/README.md` for the layout and `sync.sh` for migrating any new session jsonls (Claude Code creates them at the old path each session; the sync moves them).

## Stack

Python 3.12 (tested Linux/macOS/Windows since v1.15). No framework. stdlib + **`python-magic` (NON-Windows only — `platform_system != 'Windows'` marker since v1.15; its import-time libmagic search HANGS on Windows-without-the-DLL, which would hang `import file_observer.scanner` itself — the pure-Python fallback runs instead; NEVER re-add it unconditionally)** + chardet. Optional: PyYAML (frontmatter), olefile (MSG/DOC/XLS — required for OLE2 specialists), defusedxml (hardened XML), **pypdf** (v1.8 — object-stream PDF page_count/Info, tier 1; a stdlib fallback covers it when absent; `file-observer[pdf]`), watchfiles (`[watch]`), **pytest-timeout** (dev — `timeout=120` net so no test can hang CI). Virtual env at `.venv`. **Dev-oracle (never shipped): `exiftool`** — standalone Perl script at `scratch/tools/exiftool` (gitignored, no sudo; the v1.16/v1.17 capture-metadata parsers were gated byte-exact against it, the v1.8 `pypdf`-oracle pattern). **`requires-python = ">=3.12"` is a conservative test-pin** — the actual binding floor is **3.10, imposed by `chardet`** (not our code; details in `scratch/python_floor_analysis.md`).

## Version roadmap

- **v1.5.0 (shipped & live on PyPI):** PDF specialist head+tail read + `requires_vision` fix — surfaced by the infra-standards corpus (the corpus taught us about the observer). The PDF specialist read only the 8 KB head; page tree/`/Info`/font markers live in the trailer + compressed streams, so `page_count`/`producer` were null and born-digital PDFs were mis-flagged `requires_vision` (same bug class as pre-v0.7.1 OLE2). Fix (stdlib, no new dep): read head + bounded 128 KB tail; new provisional `pdf.text_detected`; `requires_vision` only when no-text AND image markers. Validated on 964 real PDFs: **requires_vision 310→12, page_count 9→341, producer 9→308**; object-stream PDFs return `page_count=None` (honest null). SCANNER 1.4.0→1.5.0; **LOGIC 1.3.0→1.4.0**; SCHEMA 1.3→1.4 (additive). Falsify-first; reviewed.
- **v1.4.0 (shipped & live on PyPI):** Content-shape chatlog detection gate. Adds a content-shape gate ON TOP OF the retained v1.2.4 machinery (the non-speaker stop-list is load-bearing — real-data falsification overturned the draft's "replace the stop-list" premise, corrected with per-decision sign-off). Prose rule now needs `utterance_ratio≥0.6`: a turn is an *utterance* when it has a **function word** (natural turns do; atomic data values don't — the key discriminator), ends in sentence punctuation, is multi-word, or is long. Rejects cyclic data tables (`Item:`/`Price:`, a v1.2.4 FP). Plus FP-lexicon dominance (`Added:` changelogs), density floor (sprinkled labels), version/date structure vote-against (release notes), FAQ complete-set exclusion. Provisional `content_shape` surfaced. **Honest result:** on 3,032 real files v1.4.0 == v1.2.4 (0 diff) — the gate only changes adversarial inputs, at the cost of an ultra-terse-dialogue FN; net is adversarial-FP robustness, not real-world FP reduction. Falsify-first; reviewed (in-house multi-agent + Gemini + PR bots). SCANNER 1.3.0→1.4.0; **LOGIC 1.2.0→1.3.0**; SCHEMA 1.2→1.3; method_version 8→9. The data-gated word-twisting study moves to a later minor.
- **v1.3.0 (shipped & live on PyPI):** Pure-Python content-based MIME fallback — `detect_mime` gains a magic-signature tier between libmagic and the extension fallback, so no-libmagic environments get content MIME for ~24 formats. `MAGIC_SIGNATURES` generalized to a multi-constraint matcher + expanded (archives/images/data/exe/media + RIFF→WebP/WAV/AVI, with a generic `riff_container` retained for `format_signatures` and suppressed when a sub-type matches). Short prose-colliding magics (PE `MZ`, BMP) excluded; ID3/bzip2 carry a corroborating byte (review). New provenance trigger `magic_signature_fallback`. Enriches `file_signature`/`format_signatures`/`is_polyglot` too. SCANNER 1.3.0; **LOGIC 1.1.4→1.2.0**; SCHEMA 1.2 (unchanged).
- **v1.2.x (shipped):** v1.2.0 — Chatlog generalized & hardened detection — recognizes conversational JSON/JSONL across role/content schemas (ConvoKit/ShareGPT/oasst/hh-rlhf), `.json` candidate; markdown FPs cut ~96%; `ErrorRecord.detail`; per-speaker structure (`speaker_turn_counts`/`speaker_turn_chars`/`alternation`, provisional). SCHEMA 1.1 → 1.2; LOGIC 1.0.0 → 1.1.0; method_version 3 → 4. **v1.2.1** (patch) — chatlog FP fixes from adversarial self-review: markdown structure rules require a **speaker** co-signal (date co-signal dropped); JSON detection requires ≥2 distinct speakers. LOGIC → 1.1.1; method_version → 5. **v1.2.2** (patch) — prose FP fixes found by the empirical corpus sweep: prose Rule 1 requires ≥2 distinct speakers AND ≥1 recurring speaker; stop-list expanded with header/man-page/form labels. LOGIC → 1.1.2; method_version → 6. **v1.2.3** (patch) — FAQ FP stopgap: `Question`/`Answer` added to the stop-list (cross-model reviewed by Gemini flash + pro; the deeper root — prose `Key:value` ambiguous with dialogue — deferred to a future non-count signal). LOGIC → 1.1.3; method_version → 7. **v1.2.4** (patch) — two clean wins from the in-house multi-agent code-review (issues the cross-model reviews missed): case-INSENSITIVE stop-list (closes the ALL-CAPS `FROM:`/`SUBJECT:` header hole + drops dual-listing) and `_string_has_speaker_dialogue` brought to parity with prose Rule 1 (no more JSON-string-vs-prose asymmetry). LOGIC → 1.1.4; method_version → 8. Net current: **LOGIC 1.1.4, chatlog method_version 8**, SCHEMA 1.2.
- **v1.1.0:** Corpus Intelligence (first additive minor after the freeze): `quality.duplicate_clusters` (+ counts) and `quality.specialist_stats`, both provisional, pure observation. SCHEMA 1.0 → 1.1; LOGIC unchanged.
- **v1.0.x:** v1.0.1 — import package renamed `scanner` → `file_observer` (deprecated `scanner` shim), LIMITATIONS.md, license clarity. v1.0.2 — README/PyPI positioning + honesty hardening (no scan-behavior change). Both patches; SCHEMA/LOGIC unchanged.
- **v1.0.0:** Schema freeze. Public contract binding. Backward compatibility policy. No new features — governance declaration on a proven-stable codebase. SCHEMA_VERSION 0.11 → 1.0.
- **v0.11.0:** Field stability promotions — vectors_collected, reference_tokens, per_directory_summary, email.body_chatlog, filename_patterns all promoted from provisional to stable. SECURITY.md added. No new features. SCHEMA_VERSION 0.10 → 0.11.
- **v0.10.x:** v0.10.1 adds JSONL chatlog detection (`.jsonl` files with `"type": "user"/"assistant"` role objects). v0.10.0: Human-readable scan summary, `author_aggregate` corpus vector (cross-specialist author normalization + template-default detection), `filename_patterns` vector (6 boolean subcategories on every file). Four vectors in `vectors_collected[]`. SCHEMA_VERSION 0.9 → 0.10 (additive).
- **v0.9.x:** Vector abstraction — the scanner becomes a corpus observer. `Vector` dataclass with identity digest (SHA-256). New `vectors_collected[]` manifest block. Two exemplar vectors: `chatlog` (refactored from v0.8) and `reference_tokens` (7 subcategories: at_mentions, wiki_links, code_fence_blocks, url_count, email_mentions, path_references, numeric_id_patterns). Per-file `reference_tokens` field on text files. Email body chatlog cross-cut. Per-directory aggregation in `quality.per_directory_summary[]`. Dublin Core adopted in standards tracking. All v0.9 additions provisional. SCHEMA_VERSION 0.8 → 0.9 (additive).
- **v0.8.0:** chatlog content-based specialist. First content-detected (not extension-driven) dispatch in the scanner: `is_chatlog` flag runs even with specialists disabled; `_extract_chatlog_metadata` produces 11 drift-visible fields when enabled; new `chatlog` namespace + MIME guard; `quality.chatlog_files` counter. SCHEMA_VERSION 0.7 → 0.8 (additive).
- **v0.7.x:** XLS specialist + safety_flags + ScanQuality block (v0.7.0); UTF-16/UTF-32 BOM detection + OLE2 specialists pass file path instead of 8KB sample (v0.7.1, fixes silent breakage of msg/doc/xls extraction); MSG date extraction via MAPI properties stream + MSG `from` prefers display name over Exchange legacyDN (v0.7.2). Both patches found from real-world corpus scanning.
- **v1.6.0 (shipped & live on PyPI, 2026-06-03):** production-provenance dimension — a corpus-scoped **`provenance` vector** (models on `author_aggregate`; complements it — that's WHO authored, this is WHAT-TOOL / WHEN / digitization): normalized `toolchains` (closed table — Adobe PDF Library, Autodesk HDI, Bluebeam, Microsoft, TeX, OCR fingerprints…), `production_years` (creation_date), `digitization` (born_digital/scanned/**ocr_detected**, reusing v1.5 `requires_vision`+`text_detected` + OCR-producer fingerprints). Cross-format: PDF producer + OOXML `app.xml` `<Application>`. **Pure aggregation — LOGIC unchanged (1.4.0).** Validated on `corpora_infra` (reproduced the Wayne-K facts); the re-scan also caught + fixed two extraction bugs (UTF-16BE literal `/Info` → shared `_decode_pdf_bytes` with parity-based UTF-16 detection; messy version-suffix strip). All four review legs ran (PR #36): the in-house swarm caught a determinism gap (the toolchain table now feeds `rules_hash` via `provenance_rules_fingerprint()`, **including regex flags**) + a PDF-string NUL-decode regression; the PR bots caught the flags residual + a forced-utf8 `app.xml` decode (→ raw bytes); Gemini's lone finding was refuted on triage. SCANNER 1.5.0→1.6.0; SCHEMA 1.4→1.5 (additive vector); `provenance` method_version 1. Net current: **LOGIC 1.4.0, SCHEMA 1.5**, chatlog method_version 9, provenance method_version 1.
- **v1.7.0 (shipped & live on PyPI, 2026-06-03):** structural-anchor reader. Generalizes the tail-index lesson (OLE2 v0.7.1 + PDF v1.5 both needed end-of-file reads): follow the PDF's own index (`startxref` → latest trailer → root catalog → page tree), **parsing the classic xref table for object offsets + following `/Prev`** (incremental updates), instead of v1.5's whole-file regex scan. Precise on incremental updates (root count, not max over superseded fragments — a real v1.5 bug, unit-proven) and >64 MB files (offset-seek). Xref-stream PDFs: plaintext dict → `/Info`/producer recovered; compressed page tree → `page_count` null (→ v1.8). New provisional `pdf.xref_type` (classic/stream/none) — the structural observable (no provenance trigger; specialist fields carry none); a load-bearing `STRUCTURAL_ANCHORS` dispatcher routes the reader, ZIP/OLE2 declared as already-structural (`zipfile`/`olefile`, no behavior change). **Pure specialist extraction → LOGIC unchanged (1.4.0)** — `requires_vision` marker window untouched. SCANNER 1.6.0→1.7.0; SCHEMA 1.5→1.6 (additive `pdf.xref_type`). Falsify-first (`tests/test_v1_7.py`, 5/8 failed pre-impl). Corpus (`corpora_infra`, 655 PDFs): **0 page_count/producer diff vs v1.6** (zero regression — v1.5 already covered single-revision classic ≤64 MB); honest near-zero real-data diff, the win is incremental-update precision + efficiency + altitude + the **57%-object-stream** observable that sizes v1.8. Also restored the empirical sweep (had drifted to v1.3.0): now covers `corpora_infra` + a `pdf_signal` dimension; confirmed **zero detection drift v1.4→v1.7** on ~18k files. All four review legs ran (PR #39): the standout was the review round-trip catching my own fix — a bot flagged a stale-`/Info` resurface, my fix regressed a 445 MB PDF (lost a real producer), and **corpus re-validation caught it** → the `whole is not None` authority gate fixed both. 749 tests. Net current: **LOGIC 1.4.0, SCHEMA 1.6**, chatlog method_version 9, provenance method_version 1.
- **v1.8.0 (shipped & live on PyPI, 2026-06-04):** per-format parser fork — decode object-stream PDFs. **Closes the post-v1.5 three-minor arc** (v1.6 provenance dimension → v1.7 structural road → v1.8 decode the index). The 57% of the corpus that v1.7 left `page_count=null` (page tree in an `/ObjStm`) is now filled by a tiered cascade: **`pypdf` (tier 1, optional `file-observer[pdf]`, gated like `olefile`) → stdlib in-house decoder (tier 2: zlib + PNG predictor + `/W` binary xref + `/ObjStm`) → null** — works WITH or WITHOUT the dep. Decision (RFC §4): **B with an A fallback**, *measure-first*: the pypdf-as-oracle pass showed 300/300 recoverable + a flawless oracle (282/282 agreement with v1.7), and the stdlib decoder is gated by **exact agreement with pypdf** (0 disagreements on 371 corpus PDFs; recovers 328, nulls 43 exotic — never wrong, scoped to common cases). New provisional `pdf.parser` (pypdf/stdlib/none) records the tier. **Pure specialist extraction → LOGIC unchanged (1.4.0)**; strictly additive (null→value, 0 change to any v1.7 value). Coverage on `corpora_infra` (was 355): **653/655 with pypdf, 616/655 without** (stdlib fallback recovered 261); producer 320→602. SCANNER 1.7.0→1.8.0; SCHEMA 1.6→1.7 (additive `pdf.parser`). All four review legs ran (PR #41): the in-house swarm caught pypdf missing from `ScanContext` (the v1.7 doc-ahead-of-code pattern) + redundant reads; Gemini caught a **zlib decompression bomb** on untrusted input (→ bounded `_safe_inflate`); a PR bot then caught a **bug in that fix** (false-positive on trailing bytes → keyed on `not d.eof`); a codex finding was declined with corpus data. 758 tests. Net current: **LOGIC 1.4.0, SCHEMA 1.7**, chatlog method_version 9, provenance method_version 1.
- **v1.8.1 (shipped patch — red-team hardening):** a multi-agent **red-team swarm** (construct + run real attacks on untrusted inputs, each independently verified) found 6 ways to break the never-crash / stay-bounded / deterministic / don't-escape contract; all fixed falsify-first (`tests/test_v1_8_1.py`). **DoS (the v1.8 PDF decoder — the bounded-observation caps weren't carried through to it):** `/Columns` → uncapped `bytearray` (165 B PDF → 7.85 GB) → reject `columns<1 or >len(raw)`; `/W [0 0 0]` → unbounded entry loop → reject `ew==0`; the `/Prev` chain composed to ~2 GB predictor work → aggregate `PDF_INFLATE_CAP` budget. **Crash (pre-existing):** max-length filename (`detect_sidecar`) + unreadable file (`read_sample`/`hash_file` bare `open()` outside the per-file try) each aborted the whole scan → wrapped → `ErrorRecord` (new `universal_read_failed`). **Tree-escape (pre-existing):** `iter_files` followed file symlinks out of tree (read `/etc/passwd` into the manifest) → `resolve()`-containment (like `_is_safe_zip_entry`). SCANNER 1.8.0→1.8.1; **LOGIC 1.4.0→1.4.1** (discovery skips out-of-tree symlinks; unreadable/long-name files degrade not crash); SCHEMA unchanged 1.7. 764 tests; corpus re-validated (page_count 653 unchanged, stdlib oracle 0 disagreements — caps reject only adversarial inputs). **Lesson: the bounded-observation discipline (`_safe_inflate`, `_ZIP_MAX_DECOMPRESS`, per-file ErrorRecord, `_is_safe_zip_entry`) must be carried into EVERY new parser/IO path — the red-team found exactly the spots where it wasn't.**
- **v1.8.2 (shipped patch — determinism fix):** the **decorrelated Gemini leg** red-teaming the scanner — via the newly-installed **`/maestro:security-audit` skill** (Gemini CLI extension, run through the yolo-pinned `gem-review.sh`) — surfaced 3 findings; grounded/triaged against the real code, **1 confirmed, 2 refuted**. **Confirmed (LOW):** the universal stat-failure `FileRecord` (`ERR_UNIVERSAL_STAT_FAILED`) stamped `modified_at=self.now_iso()` — wall-clock — and `modified_at` feeds `manifest_checksum`, so two scans over the same unstat-able file (deleted mid-scan / TOCTOU) produced different manifests. Fix: `modified_at=""` (empty string — matches the `checksum_sha256=""` already on that degraded record, keeping `modified_at` a non-null `str` so the **frozen contract is unchanged**; the user chose this over `None`, which would have retyped a Stable field), falsify-first (`tests/test_v1_8_2.py`; the repro caught `…469674` vs `…469909`). **Refuted by repro:** billion-laughs XML (expat ≥2.4 amplification limit *raises* + `defusedxml` present → caught, graceful) and ZIP `file_size`-forgery bomb (`>1MB` reject + `file_size`-bounded read + CRC fail-closed → `BadZipFile` → `None`). **Why it mattered: the v1.8.1 in-house swarm hunted crash/bound/escape, not *determinism* — a different model family asking a different question found a class Claude's swarm wasn't looking for. The grounding rule also earned its keep — both of Gemini's scary findings (HIGH, MEDIUM) evaporated under repro.** SCANNER 1.8.1→1.8.2; **LOGIC 1.4.1→1.4.2**; SCHEMA unchanged 1.7. 767 tests.
- **v1.9.0 (shipped & live on PyPI, 2026-06-05 — opt-in parallel scan):** `--workers N` (process pool) + a stderr progress indicator — a *performance* change with **byte-identical output regardless of worker count**, so **LOGIC (1.4.2) and SCHEMA (1.7) stay frozen** (the unchanged LOGIC *is* the falsifiable contract: `tests/test_v1_9.py` asserts identical `manifest_checksum` across workers 1/2/4). **Measure-first (RFC §6.1):** a prototype (`scratch/measure_workers.py`) measured 3.4–5.4× for a **process pool** and **net-negative** for threads (GIL-bound + the shared `self._magic` libmagic cookie is thread-unsafe) → process pool, threads/hybrid declined; the RFC explicitly allowed "decline/defer" if the payoff were marginal — it wasn't. **Load-bearing refactor:** `scan_file` made **pure** (stopped mutating `_filename_patterns_*`/`_reference_tokens_*`); those corpus counters are now derived from the records in a deterministic post-pass (`_aggregate_file_counters`), and `ProcessPoolExecutor.map`'s input-order preservation keeps `files[]` identical. `--workers`/`--progress` are runtime-only (excluded from `meta.config` like `signing_key`). Never-crash carried into the pool (any pool failure → serial-rescan fallback). Validated: workers=1-vs-4 **byte-identical on 964 real PDFs (corpora_infra), 3.12× end-to-end** (66s→21s) — Amdahl-bounded below the 4.33× per-file-pass ceiling, as the RFC predicted. SCANNER 1.8.2→1.9.0; **LOGIC unchanged 1.4.2**; SCHEMA unchanged 1.7. 772 tests. RFC-approved, falsify-first. **All four review legs clean** (in-house swarm + Gemini `/maestro:security-audit` both confirmed the contract; the empirical sweep added a permanent **Layer-A workers-equality gate** — byte-identical + no detection drift on the corpora; PR bots' 3 findings adopted — fallback reuse/shutdown, `assert`→`raise`, spawn/`__main__` guard). **Shakedown:** never-crash + workers=1-vs-8 byte-identical across **49,879 files / 13 diverse trees** (corpora, 9 code repos, chat logs). Gemini also caught a stale LOGIC comment (fixed) + a pre-existing error-path bug (stat-failure record flattens a subdir `path`) — banked then shipped as **v1.9.1**.
- **v1.9.1 (shipped patch — stat-failure path fix):** the **Gemini `/maestro:security-audit` leg's F2** (banked from v1.9.0), investigated and confirmed a real bug: the universal stat-failure `FileRecord` flattened a subdirectory file's `path` to its bare filename (`rel_path = path.relative_to(source_dir)` was computed, then `path.stat()` raised, and the except blindly reassigned `rel_path = Path(path.name)`, discarding the correct relative path). **RFC §1.18 does NOT mandate this** (it only requires a record per file, no fatal raise, structured errors) — so it was a bug, not design. Fix: compute `rel_path` defensively and **separately** from the stat I/O, so a stat failure keeps the source-relative path (`sub/ghost.txt`, not `ghost.txt`); only fall back to the bare filename if `relative_to` itself fails (path not under the tree). Falsify-first (`tests/test_v1_9_1.py`). Same edge case as v1.8.2 (TOCTOU race / mid-scan deletion on this exact degraded record). Changes the `path` value on that path → **LOGIC 1.4.2→1.4.3**; SCANNER 1.9.0→1.9.1; SCHEMA unchanged 1.7 (`path` is still a relative-posix string — now *correct*, no field retyped). 776 tests.
- **v1.10.0 (shipped — provisional-lifecycle release):** the first promotion pass since v0.11, run in both directions + a new tier. **Promoted provisional → stable (5):** `quality.duplicate_clusters` family, `quality.specialist_stats`, `errors[].detail`, `pdf.text_detected`, `pdf.xref_type`. Criterion = **settled producing logic, not age** (held: chatlog family — the non-count redesign reworks it; `pdf.parser` — the tier-2 `/Info` upgrade shifts it; `provenance` — *extended* this release, so don't promote-and-extend). **New provisional (liberal-intake stance — provisional is the cheap data-gathering tier):** `preservation` (per-file closed-table format-obsolescence, lookup-only, PREMIS-inspired, rules_hash-fingerprinted); `application` extended from OOXML to **OLE2 `.doc`/`.xls`** (read `SummaryInformation` PIDSI_APPNAME via olefile), feeding `provenance` (mv 1→2 — real-data validated: 5 corpus `.doc` → "Microsoft Word" toolchain, 0 before); a top-authors line in the human `summary` (re-display of `author_aggregate`). **New `candidate` tier (below provisional):** held observations (CAD heavy-parser, EXIF privacy, word-twisting data-gated) tracked + **sweep-instrumented, never in the manifest** — the ladder is `candidate → provisional → stable`, making promotion decisions evidence-backed at zero contract cost. All new fields preserve the v1.9 byte-identical-across-workers contract. SCANNER 1.9.1→1.10.0; SCHEMA 1.7→1.8 (promotions are a contract change, v0.11 precedent + additive provisional fields); **LOGIC unchanged 1.4.3**; provenance mv 1→2. 783 tests. RFC-approved.
- **v1.11.0 (shipped — opt-in `--watch` continuous trigger loop):** the realtime substrate the Sentinel NPC/item/lore watchers (and any equivalent downstream interpreter) consume. Rescan on FS events, emit each scan's delta as one JSONL line on stdout. **No change to what is observed** — each emitted scan is byte-identical to a one-shot `file-observer` invocation at the same FS state (the falsifiable contract: `tests/test_v1_11.py::test_watch_initial_emit_matches_oneshot_checksum`). `LOGIC` and `SCHEMA` stay frozen, same lesson as `--workers N` in v1.9. **Measure-first** (RFC §6.1, `scratch/measure_watch.py`): watchfiles vs watchdog → **watchfiles wins** decisively (1:1 event delivery vs watchdog's 6× amplification; 43ms per-event latency; 53ms cold-start). End-to-end on `chatlogs/` (~200MB, 19 files): ~50ms debounce + ~2.1s rescan ≈ **2.2s near-realtime** per chat input. **§3 decisions:** debounced batch rescan; watchfiles (new optional `file-observer[watch]` extra); stdout JSONL. **§7 defaults:** files[] excluded by default with `--watch-include-files` opt-in (keeps the stream small — the `delta` block is the load-bearing field); initial scan emits a full delta-vs-empty at startup (anchors the stream); Sentinel named as anchor consumer, design generic. **Load-bearing implementation:** kept-warm `ProcessPoolExecutor` reused across rescans (avoids cold-spawn per event — `_scan_paths_parallel` gains an `external_pool=` hook); SIGTERM/SIGINT signal handler for graceful shutdown; never-crash carried into the watch loop (per-rescan exception → stderr log, loop continues). `--watch` is incompatible with `--output` and `--format jsonl` (exit 2 with a message). SCANNER 1.10.0→1.11.0; **LOGIC unchanged 1.4.3**; **SCHEMA unchanged 1.8**. 790 tests. RFC-approved, falsify-first.
- **v1.12 → v1.17.0 (all shipped & live; full detail in the Spec list at top + `docs/HISTORY.md`):** v1.12.0 PDF residual closure (owner-locked + uncompressed-xref) → v1.12.1 red-team hardening → v1.12.2 error-code centralization → v1.13.0 `--schema` self-description → v1.14.0 promotion pass (`pdf.parser`/`application`/`provenance` → stable; SCHEMA 1.8→1.9) → **v1.15.0 cross-platform hardening (CI OS matrix; python-magic Windows-excluded; HEIC/HEIF/AVIF detection; LOGIC 1.4.3→1.5.0)** → **v1.15.1 HEIC/HEIF/AVIF recognition** → **v1.15.2 MIME-guard hardening (vnd.ms-office; `.eml` no-libmagic path; LOGIC 1.5.2)** → **v1.16.0 image capture-metadata (EXIF for JPEG+HEIC, GPS-presence→`geotagged`; LOGIC 1.6.0/SCHEMA 1.10)** → **v1.17.0 video container/track half (codec/duration/dims/creation_date for MP4/MOV/M4V; LOGIC 1.7.0/SCHEMA 1.11)** → **v1.18.0 video capture device + GPS-presence (Apple QuickTime keys make/model + gps_present/gps_source + geotagged-for-video; validated on real iPhone 16 clips; LOGIC 1.8.0/SCHEMA 1.12)** → **v1.19.0 human-readable surfaces refresh (freshened scan `summary` + new `--schema --format summary` prose; LOGIC 1.9.0/SCHEMA 1.12)** → **v1.20.0 `video.creation_date_qt` (Apple QuickTime creationdate key — capture moment WITH timezone, SEPARATE from mvhd creation_date, observe-don't-reconcile; LOGIC 1.10.0/SCHEMA 1.12→1.13, a new field)** → **v1.21.0 content-aware text recognition (recognized text no longer flagged `unsupported_extension`; the diagnostic now means "couldn't identify it"; recognition-only, no new extraction; LOGIC 1.10.0→1.11.0/SCHEMA unchanged 1.13) + a README-version drift-guard test** → **v1.21.1 provisional-designation correction (image-EXIF + the v1.17–1.20 video namespace stable→provisional in `--schema`; manifest byte-identical; + a PUBLIC_CONTRACT/CONVENTIONS contract-doc drift-guard)** → **v1.21.2 `--schema --format json` envelope shape formally committed (a build-time interface versioned by `schema_doc_version`; prompted by gazetteer as the first `--schema` consumer; + an envelope drift-guard; byte-identical scan output).**
- **NEXT: none committed — backlog (Russell steers; nothing urgent):** (1) **chatlog non-count-signal redesign** — the Sentinel thread, most strategically important, *data-gated on the tagged RPG corpus*; (2) a **promotion pass** (v1.10/v1.14 style) — image/video EXIF, `creation_date_qt`, `content_shape` are provisional→stable candidates, **but season them first** (they're recent — provisional is the data-gathering tier; don't promote young fields eagerly, same discipline as [[feedback_dont_rush_young_projects]]); (3) **Android video GPS** (`udta`/`©xyz`, needs an Android `.mp4` sample) — the last gated capture-metadata follow-up; (4) non-code **distribution/blog-post** track (channels surveyed; FO is mature enough to show — not a premature debut). **CANDIDATE-TIER (scouted 2026-06-17, sweep-instrumented via `corpus_sweep.py::_recognition_candidates` + registered CONVENTIONS §3.1, NEVER in the manifest — `scratch/scout_2026-06-17.md`):** **Candidate A** (text→**binary** content-aware recognition) = **SHIPPED as v1.22.0**. **Candidate B** (office/media **extraction**: `.pptx`/`.ppt`/`.odt`/`.ods`/`.odp`, `.mp3`/`.jp2`/`.tiff`) = **measure-first ran 2026-06-23 → DEFERRED.** 138 such files in the corpus (on par with the docx/doc/xls FO already extracts), BUT **recognition is already solid** — 135/138 typed to their correct specific MIME, 0 flagged degraded/unsupported (one headerless .mp3 aside). So Candidate B is NOT a gap-fix (recognition works) and NOT demand-driven (no consumer for office/media metadata) — it is pure additive extraction (new untrusted-binary parsers for metadata nobody asked for). Trigger to revisit: a real consumer needs office/media metadata, OR family-completeness becomes a deliberate goal. **Candidate C (corroborated-header MIME sniff)** = **SHIPPED as v1.23.2** — widened the `%PDF-` sniff window 256→1024 + added structural-token corroboration (a prefixed real PDF is kept; a deep structureless literal is rejected). **Capture-metadata red-team — IN-HOUSE LEG DONE (2026-06-22):** the v1.16–1.20 image-EXIF / video-ISOBMFF parsers (the most untrusted-binary surface since the PDF arc, never previously given a dedicated pass) were adversarially battered — static bounds audit + 21 vectors (huge IFD counts, OOB offsets, 2^64 date overflow, oversized/zero/nested boxes, moov-at-tail, hostile iloc, EXIF/APP1 bombs) — **0 violations** (the per-release bounds-guard discipline held); codified as a standing guard `tests/test_capture_metadata_hardening.py`. REMAINING: the **bestiary** decorrelated-fuzz leg (its lane, route via Russell) — the higher-confidence pass (my in-house attacks are correlated with my own blind spots) + matures bestiary's soak gate. (**Candidate B**, if built, carries the never-crash/bounded bar from the start — the v1.8.1 discipline.) Anomaly `email_envelope` 38% specialist-fail = **RESOLVED in v1.22.1** (all 8 were `.eml` files libmagic typed as text, now accepted by the guard). CI Node-20 deprecation = RESOLVED (workflows on `@v6`/`v7`/`v8`). ⏰ Gemini Code Assist (leg-4 bot) sunsets 2026-07-17 (Codex+Copilot remain). **Side-project family (each its own instance; I supply the consumer contract + stay out of day-to-day, [[feedback_stay_on_file_observer]]):** `pure*` dep-replacements (purexml/purecfb/puresniff) + `bestiary` (adversarial-soak substrate) + `gazetteer` (reference manifest-consumer). All days-old → **no publish/license/1.0/adoption-decision at this infancy** ([[feedback_dont_rush_young_projects]], [[feedback_north_star_direction_over_destination]]); purexml adoption is a held DIRECTION (bestiary M2 soak green) with the trigger deferred on maturity + Russell's production-swap gate — no clock.
- **PARKED future-discussion** (Russell, do NOT spec unprompted): note when a file has all the structural characteristics of a spec it doesn't blatantly declare, WITHOUT inferring it IS that (extends `file_signature`/polyglot/MIME-mismatch). Also: a separate **dep-elimination lab** (pkplab `purecfb`/`purexml`/`puresniff`, own instances) explores oracle-gated pure-stdlib readers to shrink file-observer's deps — parallel/speculative, does NOT steer file-observer until built+adopted.
- **Arcs.** PDF arc (v1.6/1.7/1.8 + v1.12 residual closure) done; cross-platform arc (v1.15.0/1.15.1/1.15.2) done; **capture-metadata arc COMPLETE: v1.16 image EXIF → v1.17 video container → v1.18 video device+GPS; v1.19 = human-readable refresh, v1.20 = the QuickTime `creationdate` key (the v1.18 revisit candidate, shipped).** v1.21 (content-aware recognition) was a standalone diagnostic-quality minor off the v1.20 candidate scan. **Parked / deferred:** word-twisting/authority study (RPG-tagging-gated); customer dictionaries; promotion of the chatlog family (alpha-locked per [[soft-contracts-alpha-phase]]) + `preservation`; CAD (DGN/DWG) coverage; stdlib decryption for V1-V5 PDF crypto (v1.12 left as pypdf-only); the `.eml` MIME-safety-guard relaxation (its own patch); **Android video GPS** (`udta`/`©xyz`, the last gated capture-metadata item — needs an Android `.mp4`). Additive only — v1.0 contract holds.

## Commands

```bash
# Activate venv
source .venv/bin/activate

# Install in dev mode (editable)
pip install -e ".[dev]"

# Run file-observer (after install)
file-observer

# Run directly
python src/file_observer/scanner.py

# Run all tests
python -m pytest tests/

# Run a single test
python -m pytest tests/test_foo.py::test_name -v
```

## Architecture

Single-module implementation in `src/file_observer/scanner.py`, imported as `file_observer` (v1.0.1 rename). A deprecated `scanner` package re-exports the public API with a `DeprecationWarning` for backward compatibility. No deeper package structure yet — the conceptual modules (specialists, vectors) live in the one file until a change makes splitting them worthwhile.

### v0.3 Design Pillars
1. **Capability-locked determinism** — identical inputs + identical `ScanContext` → identical outputs. Variance across environments must be explained by context (dependency versions, logic version).
2. **Signal layering** — every field is raw (direct observation), derived (computed from raw via deterministic logic), or semantic-local (reserved, opt-in). Layer membership is a contract.
3. **Structured provenance** — every derived field has a `signal_provenance` entry with `layer`, `method`, `trigger`, optional `inputs` and `detail`. Replaces process_log entirely.
4. **Bounded observation** — specialists operate within `sample_size` (8KB default). Null means "not observed within bounds," not "not present in the file."

### Capability tiers (all in Scanner class)
1. **Universal** — runs for every file: identity, filesystem metadata, checksum, path-derived fields, routing flags (`is_binary`, `requires_vision`, `requires_specialist_tool`), MIME analysis, structural file signatures (`file_signature`, `format_signatures`, `is_polyglot`).
2. **Baseline** — runs for text-like files: encoding detection (with v0.7.1 UTF-16/UTF-32 BOM short-circuit), content preview, tag extraction, frontmatter parsing, asset matching. Runs `is_chatlog` content-based detection on `.txt`/`.md`/`.mdx`/`.jsonl`/`.json` files (v1.2: generalized conversational JSON/JSONL across role/content schemas; v1.2.1: markdown structure rules require a **speaker** co-signal; v1.2.2: prose speaker-label Rule 1 requires ≥2 distinct speakers AND ≥1 recurring speaker — kills header/label-block false positives). Runs `reference_tokens` extraction (7 subcategories) on all text-eligible files. Runs `filename_patterns` detection (6 subcategories) on every file. Detection runs even when `enable_specialists=False`.
3. **Structural** — runs for text-like files: title, headings, CSV headers, document keys (JSON/YAML/XML/TOML), technology hints, filename_date.
4. **Specialist** — gated behind `ScannerConfig.enable_specialists` (default: False). Format-specific extraction with namespaced metadata. v0.6 added MIME guard (skips extraction when content MIME doesn't match expected formats) and configurable depth (`specialist_budget`, `extension_overrides`, named profiles via `SCAN_PROFILES`). v0.7 added `safety_flags` (has_javascript, has_macros, has_ole_objects, has_external_references) and the `ScanQuality` block (clean/degraded/error/mismatch/polyglot/safety counts).

### Key data flow
`Scanner.scan()` → `_build_context()` → `iter_files()` walks directory → `_scan_paths()` runs `scan_file()` per file (**serial, or a process pool when `config.workers > 1` / `--workers N` — output byte-identical either way; `scan_file` is PURE, and the `filename_patterns`/`reference_tokens` corpus counters are derived from the records afterward in `_aggregate_file_counters`; `ProcessPoolExecutor.map` preserves input order**) (each builds a `FileRecord` + `signal_provenance` + `reference_tokens` + `filename_patterns`) → register file-scoped vectors (chatlog, reference_tokens, filename_patterns) → run corpus-scoped vectors (author_aggregate) → `_build_summary()` → assembled into `ScanManifest` with `context`, `meta`, `stats`, `quality`, `routing_summary`, `delta`, `vectors_collected`, `summary`, `manifest_checksum` → serialized via `manifest_to_json()`, `manifest_to_jsonl()`, and `manifest_to_markdown()`

### Core dataclasses
- `ScanContext` — environment fingerprint: logic version, scanner version, python version, platform, dependency versions
- `ProvenanceEntry` — per-field derivation record: layer, method, trigger, inputs, detail
- `VectorRecord` — one entry in `vectors_collected[]`: vector_id, method_version, scope, rules_hash, static_tuning_hash, identity_digest, applied_to_count, summary
- `VectorRegistry` — collects vectors during a scan, produces sorted `vectors_collected[]`
- `FileRecord` — one per discovered file, all fields from spec contract + `signal_provenance` + `reference_tokens` + `filename_patterns`
- `ScanManifest` — top-level output: `context`, `meta`, `stats`, `quality`, `routing_summary`, `delta`, `vectors_collected`, `summary`, `manifest_checksum`, `files[]`
- `MimeAnalysisRecord` — content vs extension MIME comparison
- `FrontmatterRecord` — markdown frontmatter extraction
- `StructuralRecord` — structural signals (title, headings, keys, etc.)
- `ErrorRecord` — non-fatal errors captured per file per stage

### Specialist tools (semantic names — describe downstream need, not scanner implementation)
| Extension | Tool | What it extracts |
|-----------|------|-----------------|
| `.pdf` | `pdf_extraction` | page count, text streams, doc info, encrypted, pdf_version, sample_text_marker_density, text_detected (v1.5), xref_type (v1.7, classic/stream/none — read via structural anchor: `startxref`→trailer→root→page tree), parser (v1.8, pypdf/stdlib/none — the decode tier for object-stream page_count/Info) |
| `.png` | `image_structure` | width, height, bit_depth (IHDR chunk via struct) |
| `.jpg`/`.jpeg` | `image_structure` | width, height (SOF via struct) + **v1.16 EXIF** (make, model, orientation, datetime_original, gps_present, xmp_present) via the APP1 segment; bounded 1 MiB head read; `geotagged` safety_flag when gps_present |
| `.heic`/`.heif`/`.avif` | `image_structure` | **v1.16** — EXIF (make/model/orientation/datetime_original/gps_present/xmp_present) via ISO 14496-12 `meta`→`iinf`/`iloc`→`Exif`-item (version-aware `iloc`); **dims from EXIF PixelXDimension/PixelYDimension** (NOT the `ispe` tile-trap); `requires_specialist_tool` now True. (v1.15.0 detected, v1.15.1 recognized — extraction is v1.16.) |
| `.mp4`/`.mov`/`.m4v` | `video_structure` | **v1.17** — ISOBMFF container metadata: codec (stsd fourCC), duration_s (float), width/height (vide-track tkhd 16.16), creation_date (mvhd 1904-epoch→ISO8601 UTC). `_read_moov` seeks past `mdat` (moov-at-tail), bounded by `MOOV_MAX_BYTES`. **v1.18:** make/model (Apple QuickTime keys) + gps_present/gps_source + geotagged-for-video. **v1.20:** `creation_date_qt` (the `com.apple.quicktime.creationdate` key — capture moment WITH timezone, separate from mvhd `creation_date`). Android `udta`/`©xyz` GPS still gated on a sample. |
| `.msg` | `email_envelope` | subject, from, to, date, message_id, has_attachments. v0.7.1: takes file path (not 8KB sample) so olefile can follow OLE2 FAT chains. v0.7.2: date read from MAPI properties stream (PR_CLIENT_SUBMIT_TIME 0x0039 → ISO 8601), `from` prefers PR_SENDER_NAME (0x0C1A) display name over PR_SENDER_EMAIL_ADDRESS (0x0C1F) Exchange legacyDN. |
| `.eml` | `email_envelope` | subject, from, to, date, message_id, has_attachments (stdlib email.parser) |
| `.xlsx` | `spreadsheet_structure` | sheet_names, header_rows, format=ooxml, application (`docProps/app.xml` `<Application>`, v1.6) (stdlib zipfile + XML, 128KB deviation) |
| `.xls` | `spreadsheet_structure` | sheet_names, format=biff (BIFF8 BoundSheet8 records via olefile, v0.7.0). v0.7.1: takes file path. |
| `.docx` | `document_extraction` | title, author, word_count, heading_count, application (`docProps/app.xml` `<Application>`, v1.6) (OOXML ZIP, 128KB deviation) |
| `.doc` | `document_extraction` | title, author (OLE2 SummaryInformation via olefile). v0.7.1: takes file path. |
| `.rtf` | `document_extraction` | title, author ({\info} group regex on sample) |
| `.jsonl` _(v0.10.1)_ | `chatlog_signals` | JSONL conversation detection via role-bearing JSON objects (`"type": "user"/"assistant"`). Message text extracted and processed through chatlog pipeline. |
| _(content-detected)_ | `chatlog_signals` | turn_count, speaker_labels, speaker_turn_counts/speaker_turn_chars/alternation (v1.2, provisional), section_marker_count/styles, turn char stats, reference_tokens, top_capitalized_tokens, vocabulary_size_estimate. Activates by content pattern on `.txt`/`.md`/`.mdx`/`.jsonl`/`.json` (v1.2: generalized conversational schemas). |

## Known decisions

- Specialist tier disabled by default (`ScannerConfig.enable_specialists = False`)
- Preview capped at 1000 chars (`ScannerConfig.preview_max_chars`)
- Binary detection: **v0.7.1 short-circuits on UTF-16/UTF-32 BOM** at offset 0 (treats as text), then NUL byte in sample OR MIME prefix/set OR text char ratio < 0.85
- MIME detection (v1.3): libmagic primary → **pure-Python magic-signature sniff** (`_sniff_mime` over `MAGIC_SIGNATURES`, content-based, no libmagic; trigger `magic_signature_fallback`) → extension-based fallback (trigger `extension_fallback`, diagnostic error) → octet-stream. The sniff runs on the already-read head sample (`read_sample` now precedes `detect_mime` in `scan_file`).
- Encoding: chardet (confidence >= 0.50) then cascade: utf-8 → utf-8-sig → cp1252 → latin-1 → replace
- Manifest checksum excludes `scan_id` and `generated_at` (volatile fields)
- Manifest filename includes version: `manifest_v{VERSION}_{timestamp}.json`
- ScanContext excludes hostname and timestamps (not causally linked to outputs)
- Signal provenance replaces process_log — per-field, not per-tier
- Specialist tool names are semantic (describe downstream need, not scanner implementation)
- PDF sample_text_marker_density is a quantitative float, not qualitative labels
- PNG/JPEG extraction uses stdlib struct, not Pillow
- **OLE2 specialists (msg/doc/xls) take a file path, not a sample buffer** (v0.7.1) — olefile cannot follow FAT sector chains from a head-only buffer. Declared deviation from `sample_size`, bounded by file size on disk.
- **MSG date extraction parses the MAPI `__properties_version1.0` stream directly** (v0.7.2) — fixed-length properties (FILETIME) live there, not in substg streams. Reads PR_CLIENT_SUBMIT_TIME (0x0039) → ISO 8601, falls back to PR_MESSAGE_DELIVERY_TIME (0x0E06).
- **MSG `from` prefers display name (PR_SENDER_NAME 0x0C1A) over Exchange legacyDN (PR_SENDER_EMAIL_ADDRESS 0x0C1F)** (v0.7.2) — both kept in the lookup chain, but the human-readable form wins when present.
- EML extraction uses stdlib email.parser, no external dependencies
- XLSX/DOCX use 128KB deviation budget (declared exception to 8KB bounded observation)
- ZIP entries validated against path traversal (_is_safe_zip_entry), decompressed size capped at 1MB (_safe_zip_read)
- XML parsing uses defusedxml when available, stdlib fallback with documented risk
- v0.6 added `SCAN_PROFILES` (e.g. `fast_sort`) — named bundles of `extension_overrides` for common extraction depth tradeoffs
- v0.6 added `previous_manifest_checksum` and `manifest_signature` (HMAC-SHA256 with optional `signing_key`) for chain-of-custody integrity
- v0.7 added `safety_flags` (has_javascript / has_macros / has_ole_objects / has_external_references) and the `ScanQuality` block
- **`is_chatlog` is content-detected, not extension-driven** — activates for `.txt`/`.md`/`.mdx`/`.jsonl`/`.json`. Prose: speaker labels (stop-list filtered); H3 (5+) / dividers (3+) **require a co-signal** — 2+ speaker labels or 2+ date-stamped headers (v1.2, kills prose-doc false positives). JSON/JSONL (v1.2 generalized): 3+ "message-like" objects (a role key `type`/`role`/`from`/`speaker`/`author` + a content key `text`/`value`/`content`/`message`/`body`) — line-delimited, arrays, nested trees, or dialogue embedded in a JSON string; regex fallback for truncated large single-JSON. Detection runs even when `enable_specialists=False`. **Soft external contract:** `CHATLOG_SPEAKER_LABEL_RE` (`^[A-Z][a-zA-Z0-9_]{0,15}:\s`) is mirrored by Sentinel's `backend/datasets.py::build_chatlog` (their gameplay-corpus export targets this shape). Treat as an external API even though it's not in `PUBLIC_CONTRACT.md` — route any tightening through Russell to Sentinel Claude before landing. The parked "non-count signal" redesign in particular needs this.
- **v0.9 Vector abstraction** — vectors are named, uniquely-identified observation units with SHA-256 identity digests. Four vectors: chatlog, reference_tokens, author_aggregate, filename_patterns.
- **v0.10 Scan summary** — deterministic Markdown paragraph on every manifest + standalone `.md` report file.
- **v0.11 Field promotions** — vectors_collected, reference_tokens, per_directory_summary, email.body_chatlog, filename_patterns promoted from provisional to stable.
- **v1.0.1 package rename** — canonical import is `file_observer`; legacy `scanner` is a deprecated re-export shim (`DeprecationWarning`). The import path is NOT under the public contract (the manifest is), so the rename + packaging/positioning changes shipped as patches.
- **v1.1 Corpus Intelligence** — `quality.duplicate_clusters` (group by identical `checksum_sha256`, count ≥ 2) + `quality.specialist_stats` (per-tool attempted/succeeded/failed). Provisional; pure observation; no routing change.
- **v1.2 Chatlog generalized + hardened** — detection recognizes conversational JSON/JSONL across role/content schemas (`.json` candidate); per-speaker structure (provisional); `ErrorRecord.detail` added. Detection change → LOGIC 1.1.0, chatlog method_version 4. **v1.2.1/v1.2.2/v1.2.3 patches** tightened the FP rules (found by adversarial self-review + the empirical corpus sweep, cross-model reviewed): markdown structure needs a speaker co-signal; JSON needs ≥2 distinct speakers; prose Rule 1 needs ≥2 distinct + ≥1 recurring speaker; stop-list expanded (incl. FAQ `Question`/`Answer` in v1.2.3). Net: **LOGIC 1.1.4, method_version 8**. Open residual: prose `Key:value` (FAQ, tech-doc labels, i18n form fields) is ambiguous with dialogue, and the recurrence rule misses all-distinct multi-party openings — both need a future non-count signal, not more stop-list words. Word-twisting/authority study deferred (data-gated on tagged RPG corpus).
- **v1.4 Content-shape detection gate** — the "non-count signal" the v1.2.x arc pointed to, shipped as an ADDITIVE gate, NOT a stop-list replacement. The prose rule (after stop-list filtering + count floor) now requires `utterance_ratio≥0.6`, where a turn is an *utterance* if it has a function word / sentence punctuation / ≥4 words / ≥25 chars. The **function-word arm** is the real discriminator (terse dialogue has function words; atomic data values don't). Defenses: FP-lexicon dominance (changelog verbs), version-tag structure vote-against, FAQ complete-set exclusion. **Key lesson (falsify-don't-confirm in action):** the approved RFC claimed content-shape would replace the stop-list; building the adversarial corpus + diffing against 3,032 real files proved (a) the stop-list is load-bearing (doc-section labels FP'd 6 real READMEs), (b) on real data the change is a 0-diff wash — it only hardens against adversarial inputs, (c) `utterance_ratio` alone couldn't separate terse dialogue from data (function words fixed that). Premise corrected, scope confirmed with the user at each fork. **The three review layers then earned their keep:** in-house multi-agent + Gemini found a real regex bug (`\s+` crossing newlines), a backwards density floor (FN'd multi-line dialogue → **dropped**, surfaced not gated), a structure vote-against that rejected dated journals (→ version-tag-only), and a co-signal that lost label-on-own-line transcripts; the PR bots then caught doc/code drift from those mid-stream fixes. A reminder that this AI's own green-checks corpus carried builder bias. Accepted FNs (LIMITATIONS): ultra-terse contentless dialogue, all-distinct roll-call, `Q:`/`A:` interviews. LOGIC 1.2.0→1.3.0; method_version 8→9; SCHEMA 1.2→1.3 (provisional `content_shape`).
- **v1.5 PDF specialist depth + `requires_vision` fix** — found by scanning the infra-standards corpus (the corpus taught us about the *observer* — Wayne-K reflex). The PDF specialist (and `detect_requires_vision`) read only the 8 KB head, but a PDF's page tree (`/Count`), `/Info` dict (producer), and font markers live in the trailer + compressed content streams — so `page_count`/`producer` were null for ~all real PDFs and born-digital PDFs were mis-flagged `requires_vision` (no plaintext `BT`/`/Font` in the head). **Same bug class the OLE2 specialists had before v0.7.1** (fixed there by reading the file path, not a head buffer). v1.5 reads **head + bounded 128 KB tail** (stdlib, no new dep — the file is already read for the checksum): `page_count`=max `/Count` (root page tree), `/Info` literal+hex, new provisional `pdf.text_detected` (`/Font`/`BT`), `requires_vision` only when no-text AND image markers. Validated on the 964-PDF corpus: **requires_vision 310→12, page_count 9→341, producer 9→308**. Object-stream PDFs (PDF 1.5+) return `page_count=None` — honest null, NEVER a wrong/undercount value (the page tree compresses all-or-nothing); documented residual (full fix would need a real PDF parser, out of scope per the stdlib choice). LOGIC 1.3.0→1.4.0; SCHEMA 1.3→1.4 (provisional `text_detected`).
- **v1.6 production-provenance dimension** — first of the post-v1.5 three-minor arc, and the meatiest (provenance was the scanner's strongest seam per the v1.5 Wayne-K facts). A corpus-scoped `provenance` vector (models on `author_aggregate`, complements it: WHO authored vs WHAT-TOOL/WHEN/digitization): normalized `toolchains` (closed, ordered, first-match table — the vector's `dictionary_id`), `production_years` (creation_date), `digitization` (born_digital/scanned/**ocr_detected**, reusing v1.5 `requires_vision`+`text_detected` + OCR-producer fingerprints). Cross-format: PDF producer + OOXML `docProps/app.xml` `<Application>` (docx/xlsx). **Pure aggregation → LOGIC unchanged (1.4.0)** (like the v1.1 corpus-intelligence vectors); SCANNER 1.5.0→1.6.0; SCHEMA 1.4→1.5; provenance method_version 1. Observe-only boundary: counts/normalizes producers, never infers intent. Validated on `corpora_infra` (reproduced 138 Autodesk-HDI / 132 Adobe / 6 OCR / era 2002→2026); **the re-scan again caught what the synthetic ASCII tests missed** (builder bias) — UTF-16BE *literal* `/Info` strings decoded as latin-1 (→ shared `_decode_pdf_bytes` for literal+hex) and a messy version-suffix the strip regex blocked (→ permissive `.*$`). Forks at approval: corpus-only (A), PDF + cheap OOXML (B), closed table (C). **Determinism contract (review-hardened, reusable for future vectors):** the normalization table **and its regex flags** feed `rules_hash` via `provenance_rules_fingerprint()` (derived from the live table so it can't drift) — a table/flag edit MUST move the `identity_digest`, exactly like chatlog's enumerated `fp_lexicon`. The recurring bug class: any rule data (table, lexicon, flags) that gates output but isn't in the hash. **Dual-falsification framing (sharpened by Sentinel Claude exchange, 2026-06-06):** a live-derived rules-fingerprint falsifies BOTH (a) fabrication — no real (rule data, inputs) set produces the recorded fingerprint AND (b) silent rule drift — a rule edit can't pass an old fingerprint check. A stored `rule_id` reference fails (b). Use this framing when articulating the determinism contract — it's clearer than "include the rule data." Also review-hardened: `_decode_pdf_bytes` UTF-16 detection is **parity-based** (high bytes on one parity all-NUL), not "any NUL present" (which mojibake'd latin-1 producers with a stray NUL); OOXML `app.xml` is parsed from **raw bytes** (honors the encoding declaration/BOM).
- **v1.7 structural-anchor reader** — second of the post-v1.5 arc. Generalizes the tail-index lesson (OLE2 v0.7.1 + PDF v1.5 both abandoned the head-only read): the PDF index reads now **follow `startxref` → latest trailer → root catalog → page tree**, parsing the classic xref table for object offsets (resolve by offset, so a superseded incremental-update fragment can't win) and following `/Prev` (bounded, latest-wins). Replaces v1.5's "read ≤64 MB + `max(/Count anchored to /Type/Pages)`" — which over-counts on incremental updates and nulls on >64 MB. Xref-stream PDFs: read the plaintext stream dict for `/Root`+`/Info` (producer recovered by locating the regular object; the compressed offset table + object-stream page tree are NOT decoded → `page_count` null, **v1.8**). New provisional `pdf.xref_type` (classic/stream/none) — the structural observable (`none` = fell back); falls back field-by-field to the v1.5 window on a broken/absent anchor (never raises). **No `signal_provenance` trigger** — PDF specialist-metadata fields carry no per-field provenance, so a drafted `structural_anchor` trigger was dropped in review as redundant with `xref_type` (a doc-ahead-of-code catch). **Pure specialist extraction → LOGIC unchanged (1.4.0)** — the `requires_vision`/`text_detected` marker window is deliberately untouched (changing a routing flag would force a LOGIC bump). `STRUCTURAL_ANCHORS` table keys off the v1.3 format ID and is **load-bearing** (a `_read_structural_index` dispatcher routes `pdf`→`trailer_pointer`→`_pdf_anchor`; another review catch — it was decorative); ZIP/OLE2 are entered as documentation (already structural via `zipfile`/`olefile`) — no behavior change. Read-once: the anchor resolves every object by slicing the already-read whole-file bytes (seek only for > cap) — a review efficiency fix. SCANNER 1.6.0→1.7.0; SCHEMA 1.5→1.6 (additive `pdf.xref_type`). Forks resolved at approval: A index-only (LOGIC unchanged), B locate xref-stream + read dict for /Info only, C add `pdf.xref_type`, D OLE2/ZIP document-only. **Honest result:** corpus diff is **zero** (v1.5 already handled the common case) — value is incremental-update precision + efficiency + altitude + the 57%-object-stream observable sizing v1.8. The "run against everything" check that validated it also **restored the empirical sweep** (stale at v1.3.0 → live at v1.7, now tracking `corpora_infra` + `pdf_signal`) and fixed a pytest-scoping bug that was polluting the corpora with `__pycache__`.
- **v1.8 per-format parser fork** — third and final of the post-v1.5 arc; decodes the object-stream PDFs (page tree in an `/ObjStm`) v1.7 left `page_count=null` (57% of the corpus). **Decision: B with an A fallback** (a tiered cascade), chosen by the user after the RFC laid out A=stdlib-only / B=optional pypdf / C=hybrid. Cascade: **`pypdf` (tier 1, optional `file-observer[pdf]`, gated import like `olefile`) → stdlib in-house decoder (tier 2) → null.** So recovery works with OR without the dep. **Measure-first discipline (RFC §6):** the build's first step was a pypdf-as-oracle pass (300/300 recoverable, 282/282 agreement with v1.7 — a flawless oracle), BEFORE writing the stdlib decoder. The stdlib decoder (`_pdf_via_stdlib`: zlib + `_png_predictor_undo` + `_pdf_xref_stream_map` over `/W`/`/Index`/`/Prev` + `_pdf_objstm_extract`) is **scoped to common cases** and gated by **exact agreement with the pypdf oracle** — it may return null on exotic inputs (avg/paeth/TIFF predictors, ~12%), but NEVER a different value (0 disagreements on 371 corpus PDFs; the test `TestStdlibOracleParity` enforces it). New provisional `pdf.parser` (pypdf/stdlib/none) records the tier — the decode observable (complements `xref_type`, the structural observable). **Pure specialist extraction → LOGIC unchanged (1.4.0)**; strictly additive (fills nulls, never changes a v1.7 value — corpus-verified 0 changes). `pypdf` joins `ScanContext` when present (like libmagic/chardet); core install stays lean. SCANNER 1.7.0→1.8.0; SCHEMA 1.6→1.7. Coverage on `corpora_infra` (was 355/655): **653 with pypdf, 616 without** (stdlib recovered 261); producer 320→602. Residuals (rescoped by v1.12's corpus re-scan — original framing was stale): 2 **owner-permission-locked encrypted** PDFs (Caltrans 2025 specs; AES-256/V5; the gate at scanner.py:3082 was the actual blocker, not the inner decrypt — closed by v1.12) + 42 **uncompressed xref-stream** PDFs (the actual "~12% gap" — predictor extension was theoretical; closed by v1.12 §3.3 PIVOT). >64 MB still needs pypdf. Closes the arc — see v1.12 for the residual-closing release.
- **v1.9 opt-in parallel scan (`--workers N`)** — scan files concurrently via a process pool, with the absolute contract that the manifest is **byte-identical regardless of worker count** → **LOGIC and SCHEMA stay frozen** (the unchanged LOGIC is the falsifiable proof; `tests/test_v1_9.py` pins identical `manifest_checksum` across workers 1/2/4). **Measure-first** (RFC §6.1, `scratch/measure_workers.py`): a prototype chose a **process pool** (3.4–5.4×; threads net-negative — GIL-bound + the shared `self._magic` libmagic cookie is thread-unsafe; hybrid moot). The load-bearing change: **`scan_file` made PURE** (stopped mutating `_filename_patterns_*`/`_reference_tokens_*`) — those counters are derived from the records afterward (`_aggregate_file_counters`), and `ProcessPoolExecutor.map`'s input-order preservation keeps `files[]` identical. `--workers`/`--progress` are runtime-only (excluded from `meta.config`, like `signing_key` — no causal link to output). Never-crash carried into the pool (any failure → serial-rescan fallback that **reuses** the records already returned). Validated: byte-identical workers=1-vs-N on real corpora + a **49,879-file / 13-tree shakedown**, 3.12× end-to-end; a permanent **Layer-A workers-equality gate** added to `corpus_sweep.py`; all four review legs clean. SCANNER 1.8.2→1.9.0; **LOGIC unchanged 1.4.2**; SCHEMA 1.7. **v1.9.1** (Gemini-`/maestro:security-audit` F2 follow-up): the universal stat-failure record's `path` no longer flattens a subdirectory file to its bare filename (compute `rel_path` defensively + separately from the stat I/O; `path.absolute()` for direct CWD-relative calls) — **LOGIC 1.4.2→1.4.3**, SCHEMA unchanged (`path` still a relative-posix string, now correct). Net current: **SCANNER 1.9.1, LOGIC 1.4.3, SCHEMA 1.7**.
- **v1.10 provisional lifecycle + the `candidate` tier** — the durable governance addition. The field ladder is now **`candidate → provisional → stable`**, and the promotion criterion is **settled producing logic + evidence of value, NOT age** (the held set proves it: chatlog/`pdf.parser`/`provenance` are old-enough but held for substantive reasons). The novel piece is the **`candidate` tier *below* provisional**: held observations are tracked + cheaply **harvested in the sweep apparatus** (`corpus_sweep.py`) — *never in the public manifest, never a new scanner parser surface* — so the decision to promote a candidate up to provisional is evidence-backed at **zero contract cost**. Intake stance (decided with Russell): **be liberal admitting cheap observe-only provisional fields** (provisional is the data-gathering tier — "no confirmed consumer yet" is what it *probes*), but a *heavy new parser* still gets the measured candidate treatment (the v1.8.1 never-crash/bounded obligation doesn't relax for provisional). Promotion is a SCHEMA-level change (1.7→1.8, v0.11 precedent) even with no value change; LOGIC unchanged. Registry: CONVENTIONS §3.1.

## Test fixtures

`tests/fixtures/` contains sample files across formats (.md, .pdf, .txt, .csv, .html, .yaml, .xlsx, .png, .docx, .rtf, .json, .mdx, .jpg). Chatlog fixtures in `tests/fixtures/edge_cases/`.

Test suite: **1000+ tests** (run `python -m pytest` for the exact current count — it moves with every addition, so this doc doesn't pin it) across the always-present suites (`test_unit.py`, `test_integration.py`, `test_golden.py`, `test_edge_cases.py`, `test_packaging.py`, `test_fixture_coverage.py`) plus a `test_v1_<minor>.py` file per release since v1.1 — **currently through `test_v1_32.py`** (+ the patch suites `test_v1_2_1.py`–`test_v1_2_4.py`, `test_v1_8_1.py`/`.2`, `test_v1_9_1.py`, `test_v1_12_1.py`/`.2`, `test_v1_15_1.py`/`.2`, `test_v1_25_1.py`). `test_packaging.py` now carries **several committed-doc drift-guards** so docs can't silently lag the build: `test_version_surfaces_stay_in_sync` (pyproject + the scanner.py docstring Version/Schema/Spec lines == constants), `test_readme_version_references_current` (README example-manifest + Version/Schema cells + current-minor RFC link), `test_contract_docs_version_references_current` (PUBLIC_CONTRACT §3 + CONVENTIONS §1.1), `test_readme_lists_every_specialist_format` (every `SPECIALIST_TOOLS` ext in the README list), and `test_all_extra_covers_every_runtime_extra` (the `[all]` extra is the union of the runtime extras). The PROSE that's still hand-maintained (CLAUDE.md narrative, LIMITATIONS, README headline counts) is the unguarded surface — audit it per release. **A "documentation audit" means ALL tracked docs (`git ls-files`), not just README/CLAUDE** ([[feedback_audit_means_all_docs]]): distinguish HISTORICAL/frozen (per-version RFCs, `COMPLIANCE-*`, `archive/`, the per-release roadmap log here — never "update to current") from LIVE/must-be-current; sweep for broken links, stale RFC `Status:` lines + SECURITY supported-versions + hardcoded counts, and tracking-doc section-consistency (STANDARDS_TRACKING, the candidate registry).

## Public-facing surfaces (added in the v1.13 launch — keep current, never duplicate)

Beyond `src/` and `tests/`, the repo carries the consumer-facing layer. **The one rule across all of it: link to the canonical content, never hand-maintain a second copy that can drift** — the v1.13-era README link/version drift (broken `docs/` paths, stale versions, a wrong `../LICENSE`) was exactly that failure, found and fixed in a dedicated freshness pass.

- `docs/TUTORIAL.md` — the canonical 9-section guided tour; external blog posts deep-link its **stable anchors**, so don't churn the headings.
- `examples/` — runnable, self-contained `01`–`07`, one per concept; each ships its own input + `run.sh` + a README with a **salient-output excerpt verified against a live scan** (never a committed full manifest — those carry `scanner_version` + `manifest_checksum` that rot). Run outputs (`*/out/`) are gitignored; `run.sh` clears `out/` at start.
- `docs/assets/` — brand set: `logo.png` + variants (`logo-wordmark`/`logo-mono`/`favicon`), `pipeline-diagram.png`, `og-card.png`, `logo-motif.png`. All **true-transparent** PNGs (verify `mode == RGBA`) except `og-card.png` (solid by design — social cards render full-bleed). Logo-derived variants are **composited from the real mark**, not regenerated (a generator can't reproduce an exact reference).
- `site/` — the static `file-observer.russalo.com` landing page: one `index.html` + its own asset copies, **version-agnostic**, links out to the GitHub docs. Served by Caddy `file_server`; DNS/cert/vhost are **infra = tailnet's lane**, not this repo (ready snippet in `site/README.md`).
- `README.md` (repo root since the PR-#53 move) leads with the **plain-language positioning**; **Apache Tika** is demoted to a parenthetical (a recognition token, not an explanation) and a Trademarks section disclaims affiliation.
- **Distribution/run-anywhere surfaces (the v1.26→v1.28.1 adoption-by-lowering-barriers track, 2026-06-27):**
  - `docs/manifest.schema.json` — the GENERATED JSON Schema (draft 2020-12) of the manifest (v1.27); regenerate via `--schema --schema-format json-schema` on any manifest dataclass change (drift-guarded: committed==regenerated + a real manifest validates; `jsonschema` is DEV-only). The any-language consumer contract.
  - `Dockerfile` + `.dockerignore` + `.github/workflows/docker-publish.yml` — the container image (builds FROM SOURCE; GHCR `ghcr.io/russalo/file-observer`, PUBLIC, auto-builds+tags on each release; non-root; `CMD --stdout .`).
  - `action.yml` + `.github/workflows/action-selftest.yml` — the composite GitHub Action (`uses: russalo/file-observer@<tag>`; isolated venv, cross-platform, `--stdout` to a file; self-test is a matrix `uses: ./` on ubuntu+windows — KEEP the windows leg, it caught a real Windows bug).
  - The run-anywhere front doors are documented in the README install section: `pip install "file-observer[all]"`, `uvx`/`pipx run`, `docker run`, the Action. **`--stdout` is the pipe-friendly one-shot mode that makes all of this composable.**

Distribution status (channels, sends, the awesome-python star gate) is tracked in `scratch/tutorial_examples_branding_RESUME.md` — internal, out of git per [[feedback_internal_decisions_out_of_git]].
