# Sample Dated Note — 2026-01-15

Synthetic, whitelisted (`sample_*`) fixture for `test_filename_date_extraction`:
the `YYYY-MM-DD` in the filename populates `structural.filename_date`. Committed
so the test + CI don't depend on untracked local fixtures.
