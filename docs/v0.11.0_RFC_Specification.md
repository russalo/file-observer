# Scanner v0.11.0 RFC Specification

**Project:** Scanner
**Target Version:** 0.11.0
**Status:** Approved
**Date:** 2026-05-27
**Extends:** v0.10.0 RFC Specification (with v0.10.1/v0.10.2 patches)

---

## 1. Overview

### 1.1 Purpose

v0.11 promotes provisional fields to stable and adds SECURITY.md. No new features, no new vectors — this release hardens the manifest shape in preparation for v1.0.

### 1.2 Design Principle

> Fields earn stability through survival. A field that has been present across multiple releases and patches without shape changes has proven itself. Promotion is recognition, not invention.

### 1.3 Normative Language

Per BCP 14 (RFC 2119 / RFC 8174).

---

## 2. Provisional → Stable Promotions

### 2.1 Promoted fields

| Field | Location | Introduced | Patches survived | Decision |
|---|---|---|---|---|
| `vectors_collected[]` | `ScanManifest` | v0.9 | v0.9.1, v0.9.2, v0.10.0, v0.10.1, v0.10.2 | **STABLE** |
| `reference_tokens` | `FileRecord` | v0.9 | v0.9.2, v0.10.0, v0.10.1, v0.10.2 | **STABLE** |
| `quality.per_directory_summary[]` | `ScanQuality` | v0.9 | v0.10.0, v0.10.1, v0.10.2 | **STABLE** |
| `specialist_metadata.email.body_chatlog` | `email` namespace | v0.9 | v0.10.0, v0.10.1, v0.10.2 | **STABLE** |
| `filename_patterns` | `FileRecord` | v0.10 | v0.10.1, v0.10.2 | **STABLE** |

`summary` was already marked stable at introduction in v0.10.

### 2.2 What promotion means

Once promoted:
- The field MUST be present in every manifest (or follow its null/empty semantics)
- The field MUST NOT be removed or renamed without a MAJOR version bump
- The field's type MUST NOT change without a MAJOR version bump
- New sub-fields may be added in MINOR releases (additive only)

### 2.3 Remaining provisional fields

After v0.11, the only provisional fields are:
- `format_signatures` — internal magic signature scan results
- `is_polyglot` — derived from format_signatures

---

## 3. SECURITY.md

New file at project root (`SECURITY.md`). Full contents:

### 3.1 Scope

Scanner is an **observation-only** tool. It reads files and emits metadata. It never:
- Executes file content
- Modifies source files
- Opens network connections
- Runs embedded scripts, macros, or code found in scanned files

The scanner does not execute file content. Its attack surface is limited to parsing. A malformed file can cause a specialist to raise an exception or return incorrect metadata. Because the scanner relies on native dependencies (e.g., libmagic), parsing of untrusted input carries residual risk from dependency vulnerabilities. Keep dependencies updated and consider sandboxing when scanning untrusted files.

### 3.2 Reporting Vulnerabilities

If you discover a security issue, please report it privately:

- **Email:** security@russalo.com
- **Do not** open a public GitHub issue for security vulnerabilities

We will acknowledge receipt within 48 hours and provide an initial assessment within 7 days.

### 3.3 Dependency Security

| Dependency | Role | Risk mitigation |
|---|---|---|
| `python-magic` / `libmagic` | MIME detection | Read-only, no execution. Fallback to extension-based detection if unavailable. |
| `chardet` | Encoding detection | Read-only. Fallback to encoding cascade if unavailable. |
| `olefile` | OLE2 parsing (.msg/.doc/.xls) | Read-only stream access. Files opened via path (not buffer) for OLE2 FAT chain traversal. |
| `defusedxml` | XML parsing | Used when available to block entity expansion attacks. Falls back to stdlib `xml.etree.ElementTree` when not installed (documented risk — entity expansion not mitigated in fallback mode). |
| `PyYAML` | Frontmatter parsing | `safe_load` only. Fallback to string splitting if unavailable. |

### 3.4 Bounded Observation

All specialist extractors operate within declared bounds:
- **Default sample:** 8KB read from file head
- **OOXML deviation:** 128KB for ZIP-based formats (DOCX, XLSX) — declared, bounded
- **OLE2 deviation:** full file via path for .msg/.doc/.xls — declared, bounded by file size on disk
- **ZIP traversal:** path components validated against traversal attacks (`_is_safe_zip_entry`), decompressed size capped at 1MB (`_safe_zip_read`)

### 3.5 Safety Flags

The scanner detects structural indicators that may represent security concerns:
- `has_javascript` — PDF contains JavaScript markers
- `has_macros` — DOCX contains VBA macro binary
- `has_ole_objects` — RTF contains embedded OLE objects
- `has_external_references` — XML contains external entity declarations

These are **observations, not assessments**. The scanner reports what it sees. Consumers apply their own threat model.

### 3.6 Supported Versions

| Version | Supported |
|---|---|
| 0.11.x | Yes |
| 0.10.x | Until 0.11 ships |
| < 0.10 | No |

---

## 4. Schema Impact

Schema version: `0.10` → `0.11`. No structural changes — the fields already exist. The bump reflects their stability status change.

No fields added. No fields removed. No type changes.

---

## 5. Implementation

### 5.1 Version constants
- `SCANNER_VERSION = "0.11.0"`
- `LOGIC_VERSION` stays `"0.10.1"` (no routing changes)
- `SCHEMA_VERSION = "0.11"`

### 5.2 PUBLIC_CONTRACT.md
- Move promoted fields from §2.4 Internal Field Sets to stable tables in §1.x
- Add stability annotations with `since 0.11`

### 5.3 Documentation
- CLAUDE.md, CONVENTIONS.md, README.md version updates
- HISTORY.md v0.11 row
- Module docstring

---

## 6. Acceptance Criteria

v0.11 is complete when:
- All five fields promoted in PUBLIC_CONTRACT.md
- §2.4 Internal Field Sets reduced to format_signatures + is_polyglot
- SECURITY.md exists at project root
- Schema version is "0.11"
- All v0.10 tests pass unchanged
- `schema_version` is `"0.11"`
