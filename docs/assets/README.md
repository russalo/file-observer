# Brand + diagram assets for file-observer

Generated via the prompts in `scratch/gemini_asset_prompts.md` (positioning-guarded:
no eye / camera / surveillance motifs). Palette: deep slate `#2b3a44` + teal `#2a9d8f`.

| File | What it is | Used in |
|---|---|---|
| `logo.png` | Aperture-to-grid mark (raw input → structured output) | README header; repo favicon/social source |
| `pipeline-diagram.png` | Where file-observer sits: files → deterministic observation (checksum-sealed) → one JSON manifest → many consumers | README hero; `docs/TUTORIAL.md` §1 |

Pending variants (optional polish): wordmark lockup, monochrome logo, favicon crop, OG/social card.
